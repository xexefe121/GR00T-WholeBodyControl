# Public snapshot inventory

The manifest covers newly published source changes and local experiment files; unchanged source history remains in Git. Large files are attached to the matching GitHub release. Exclusions and public-copy credential edits are explicit.
