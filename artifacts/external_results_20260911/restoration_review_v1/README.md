**Restoration acceptance has both required feasibility gates. No bypass or actual-state/history mutation was found in the reviewed path.** This is a read-only source review, not a successful-continuation claim. Seven inspected source files match both `hard_feasibility_restoration_continuation_3740_v1/request.json` and that run's frozen snapshots byte for byte; hashes are in `reviewed_source_hashes.json`. No solve, inference or physics call was made for this review.

| Requirement | Inspection result |
|---|---|
| Same actual planning state | Continuation constructs `actual = concatenate(data.qpos, data.qvel)` once before scoring. The restoration rollout, restoration iLQR, hard nominal recheck and subsequent main solve all receive that same value. The independent oracle receives the still-current actual `MjData`; no restoration branch calls actual `mj_step`, `mj_setState`, `mj_forward` or history update. |
| Shifted seed is a copy | At each replan, warm targets shift five controls and receive five reference targets. Restoration validates initial H30-by-23 finite native-bounded targets and stores a copy; subsequent triggers assign `warm.copy()`. Its iLQR receives another copy. The original executed-history/state arrays are never used as mutable search variables. |
| Main objective unchanged | `Native23RestorationTracker` is a separate object. Its seed-search merit is physical margin/speed/height/tilt plus `1e-4` deviation from the original shifted warm commands. The main tracker retains all-joint margin 0.05/weight 2000, relative-foot weight 400 and the existing complete tracking residual/control cost. Reference frames remain `control + 10`; neither source timing nor motion arrays are changed. |
| Both certification gates mandatory | Runner lines 172–183 first reroll the proposal through the main hard tracker. Only finite checked cost opens the independent `inspect_native_segment(native, data, proposal_targets, ...)` call. Only `oracle['feasible']` installs a restoration seed. The oracle deep-copies full actual `MjData`, checks all 300 native 2 ms steps and checks the original integration state/warnings remain unchanged. |
| Restored merit cannot become execution objective | The main hard iLQR immediately rerolls/revalidates the accepted proposal and optimizes the original tracking objective. Returned restoration gains are discarded. Returned restoration states are not trusted as the main rollout; hard-checked states are used. Thus the legacy soft solver's returned-gain semantics cannot leak into executed feedback through this path. |
| NaN/all-infinite rejection | Restoration maps every nonfinite rollout total to positive infinity. Nonfinite initial merit skips the solve; an all-infinite line search cannot improve a finite incumbent. Regardless of soft-search internals, nonfinite/invalid final proposals cannot pass the hard tracker and independent native oracle. Main hard iLQR preserves its feasible incumbent and typed no-feasible exit. |
| No physical or history advance during search | Restorer owns separate batch data. BFM proposal copies its observation history/action and uses private simulation. Only after the restored/main plan is complete and the next target passes the full-copy imminent-control check does the runner call `fresh.record_control`, then actual `mj_step`. No fallback supplies root forces or state correction. |
| Honest bounded request | Request is 100 controls/2.0 s from control 3740, H30/commit5, no full-source/hardware claim. Restoration does not reduce the requested length. `completed` requires `done == args.controls` and no failure. Consequently a no-feasible or imminent-control rejection cannot be reported as successful 100-control completion. A 600 ms restored seed is not recursive recovery qualification. |

Open reporting/provenance issues reported to the owner; these are not observed physical failures of the active run:

1. **Partial controls counted as completed.** Runner lines 235–245 break the physics loop on a failing substep, append the control and increment `done`. The final `completed_controls`, `final_control` and formula-based `final_source_seconds` therefore count an attempted partial control as full. `completed` remains false because failure is populated, so this does not manufacture a successful run. Report full controls, attempted controls and partial substeps separately; derive exact elapsed source time from completed physics steps.
2. **Unexpected preview/actual mismatch can lose final evidence.** Lines 222–223 assert actual q/dq/actuator-force equality before appending that step's raw trace; the assertion is uncaught. Such a mismatch aborts before final report/trace serialization. Retain the raw actual step first and convert mismatch to a structured failure through the ordinary finalization path. `trace.partial.npz` otherwise only preserves the preceding completed planning batch.
3. **Actual expected clock still uses multiplication.** Line 233 checks `initial_time + N*.002`. At this fixture's start time, exactly 1,000 ordinary binary64 additions differ from that formula by only `-4.66116e-12` s, below the unchanged `1e-10` threshold, so this cannot explain the bounded 2 s result. Larger accepted `--controls` can eventually encounter the previously established normal-roundoff false rejection. Use a separate expected-time accumulator initialized once, advanced by `.002` and never synchronized from observed post-step time; keep ideal formula difference as a diagnostic.
4. **Restored proposal certificates are not independently reproducible from the final artifact alone.** Plans preserve merit and oracle summaries, but do not save each restored H30 target array or bind it by hash to the full actual integration state at its trigger. The final main-MPC commands need not equal that seed. Preserve per-trigger proposal targets and full integration state/hash, with control/history linkage, to allow each 300-step certificate to be replayed later. This is an evidence-retention gap, not a missing live acceptance check.

The merit itself samples knot states and permits intermediate physical violations during its private search. That is declared exploratory behavior; the mandatory nominal every-2-ms and full actual-state native certifications are what decide seed use. Passing those gates still does not establish original-intent tracking, a safe terminal continuation, real-time execution or recursive feasibility.
