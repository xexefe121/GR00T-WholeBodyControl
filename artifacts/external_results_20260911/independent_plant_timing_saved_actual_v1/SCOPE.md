# Future instrumented-clock saved audit helpers

Source preparation only; no actual clock/audit request, clearance or dispatch. The corrected 25-file sidecar auditor remains frozen. The future launcher consumes its actual source review from the completed-run request and verifies the review's digest, full source map and exact preparation digest. That review must itself be an explicit pinned request input. No placeholder future review is accepted.

The completed pending-result saved-audit PowerShell template and owner verifier are byte-identical. Known raw exit, exact concrete review, CreateNew lock, hidden acquired process handle, mandatory `-LaunchReceiptSha256`, shared-delete stream hashing, no automatic retry, process absence and full original input/output ownership remain unchanged. Actual invocation will also use `-NoProfile -NonInteractive` when selected.

Only the source/output namespace and reviewed-source metadata change. Sidecar data, timing gaps, original stages/native counters, failures and partial captures are audited by the separately reviewed math. Evidence integrity may pass while component, timing and physical qualification remain false. Actual saved audit requires completed instrumented producer evidence and later root concrete selection; none exists yet.
