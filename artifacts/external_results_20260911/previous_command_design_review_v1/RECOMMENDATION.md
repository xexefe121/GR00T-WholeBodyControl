Recommend one fixed, saved-state **previous-command memory intervention audit before another learner fit**. The current evidence supports investigating this input path; it does not identify it as the cause of the early failure. No new model, physics or fitting calls were made for this review.

The 70000 head reduced nominal error and velocity-chord loss, yet failed at control261/substep6. At control251, before any clipping, its target error is 0.108521 rad RMS against the nominal teacher and 0.119007 against the same frozen committed map evaluated on the actual state. At252 those errors are 0.289007 and 0.296782. BFM-base departures are much smaller than head departures. The first target clip is253; prior/action-history inconsistencies caused by clipping begin254/255. A runtime change to applied-target history cannot account for the initial251/252 amplification.

The saved final Jacobian at the exact shared activation250 has conditional prior-target norm6.124 and spectral radius1.499. Those are conditional algebraic quantities with BFM/plant/history held fixed, not closed-loop stability eigenvalues. The most relevant available linearization is also unreliable away from250: multiplying that saved Jacobian by the observed251 feature departure predicts a0.107430-rad RMS head change, but its vector error is0.130169rad, larger than the actual0.107968-rad change. The prior blocks contribute0.013879rad RMS to that approximation and the velocity block0.098308rad. These coupled, inaccurate linearized contributions do not establish causal responsibility.

Masking the existing head's raw-prior features would change inputs outside its trained contract while leaving the derived previous-target features and BFM's own memory response. Removing both direct paths and retraining would still leave prior information through the BFM-base input; its conditional total-target norm is4.583 at250. Prior-command augmentation is a more coherent way to regularize a demonstrated response, but the evidence does not yet justify a large prior-only generation/fit. Merely changing one feature block while retaining a stale BFM base is not coherent augmentation.

Use the12 already saved actual precontrols250..261 and the corresponding qualified query250 teacher priors. Freeze all inputs, actual frozen-map targets and current70000 head before execution. At each state, first recompute the unchanged baseline using the original WSL batch1 runtime, and require byte equality with saved state52, BFM base/action, features, head delta, combined raw action and applied target. Cache the original backward latent once per state. This is12 backward,12 baseline actor and12 baseline head calls.

Then perform one predetermined intervention per state: replace only the current previous raw action by the same-clock teacher's previous raw action; keep measured qpos/qvel, reference frame and actor lag-history input fixed. Recompute both the actor's `last_action` input and the derived clipped previous-target feature from that same value, recompute the BFM base with the cached latent, and run the unchanged residual head. This adds12 actor and12 head calls: **60 total graph calls**, with separate attempted/returned caps of12 backward,24 actor and24 head. No optimizer, dynamics, candidate selection or connected control. Control250 must be an exact no-op, since its original inputs already match the teacher.

The history convention matters: at control k, `BFMHistory.before_update` returns stored observations through k−1, whose newest `actions` value is command k−2. The current previous action is command k−1 and enters the actor separately. Do not overwrite history.actions[0] with this intervention. The staged history would receive the changed current action for a future call, but this isolated audit does not advance time or consume that staged history.

Report signed and RMS changes in base, residual, unclipped/applied target, and error against the already computed frozen-map target at the same actual state. Controls251 and252 are the prespecified primary evidence, since clipping has not yet occurred; retain all12 results. If coherent prior replacement does not materially move both early commands toward those targets, a prior-only learner intervention lacks its proposed mechanism. If it does, a later finite prior-command chord design can be justified, with the unchanged anchor and velocity objectives preserved. Do not select a new checkpoint, alter feature normalization, relax native limits or launch another fit as part of this diagnostic.

This deliberately changes policy memory while holding the measured state fixed. It is a causal input intervention on the frozen controller, **not a physically realized previous-command trajectory** and not a qualified training dataset. Physically coherent previous-command perturbation labels would additionally need the actual next qpos/qvel, clock and history produced by that command, plus independently checked teacher continuation. Do not substitute same-state memory probes for that evidence or infer simulation success from reduced target error.

Evidence: saved chronology7a24e283; actual frozen-map comparison82a743ee; final source/fit reviewdc0ba6b0; additional saved-Jacobian arithmetic57b08fa0. All complete paths and SHA256 values are bound in `proposal.json` and `saved_assessment.json`.
