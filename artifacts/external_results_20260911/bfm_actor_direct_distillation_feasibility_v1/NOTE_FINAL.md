Direct actor distillation is implementable from local weights and saved inputs, but the unchanged actor cannot reproduce every expert target. This is an unselected fallback hypothesis, not a recommended replacement for the running physical-response experiment.

The pinned actor has 23,123,955 float32 parameters in 36 tensors. Its inputs are state52, current previous-action23, four-sample history300 and goal latent256. Separate state and state-plus-goal embeddings feed a width2048 network with four residual blocks, LayerNorm/Mish and a final tanh23. The local inference package contains all actor weights, backward-map weights and frozen observation normalization statistics (54 tensors total). The graph ends in Tanh. Sources: [functional actor](Z:/codex/GR00T-WholeBodyControl-sonic-transfer-23dof/gear_sonic/utils/g1_true23_bfmzero_inference.py), [ONNX exporter](Z:/codex/GR00T-WholeBodyControl-sonic-transfer-23dof/gear_sonic/utils/g1_true23_bfmzero_onnx.py), [original architecture](Z:/codex/GR00T-WholeBodyControl-sonic-transfer-23dof/artifacts/g1_true23_six_hour_replan_20260910_v1/bfmzero_sources_v1/humanoidverse/agents/nn_models.py).

Every required input already exists for all3057 qualified moving centers, including exact flattened/named history and saved goal_latent. The actor sees the full history directly; the current residual head sees history only through BFM base output. Updating the actor could therefore learn a different history-dependent response. This does not prove history compression caused the observed failures: the original actor already consumes that history, the frozen goal latent compresses the larger reference, and the expert also depends on hidden planner state. No richer-observation or stability claim is established.

Normalization must preserve the actor's original per-input frozen BatchNorm running mean/variance (no affine parameters, epsilon1e-5), rather than the residual head's feature mean/std. The current state uses relative joint positions, unscaled joint velocities, projected gravity and body gyro scaled0.25. History retains four prior75-component terms, newest first within alphabetically ordered groups. Current last_action and historical actions are the original post-rescale values, with no clamp. The saved goal latent has dimension256 and norm approximately16. Sources: [observation/history contract](E:/codex-artifacts/sonic23_teleop_resume_20260911/velocity_chord_student_v1/source_snapshot_v3/gear_sonic/utils/g1_true23_bfm_seed_observations.py), [goal and action convention](E:/codex-artifacts/sonic23_teleop_resume_20260911/velocity_chord_student_v1/source_snapshot_v3/gear_sonic/utils/g1_true23_mjbatch_bfm_seed.py), [normalizer source](Z:/codex/GR00T-WholeBodyControl-sonic-transfer-23dof/artifacts/g1_true23_six_hour_replan_20260910_v1/bfmzero_sources_v1/humanoidverse/agents/normalizers.py).

The exact actor output contract is raw=tanh(...), action=5*raw, target=nativeclip(default_q+action*0.25*training_effort/kp). Thus an expert position would require raw=(expert_target-default_q)*kp/(1.25*training_effort). Across the saved3057 centers, 90/70311 components in88 rows require |raw|>1 (old31 components/30rows, query1 31/30, query250 28/28). Maximum required |raw| is1.3391148514. Even an ideal actor at the closed action bounds has an aggregate target RMSE floor0.00768787rad and maximum single-component miss0.59457254rad; this optimistic projection is a coverage calculation, not generated supervision. Clipping/averaging/dropping those expert targets would silently change the task. The saved inputs also contain 90 current-prior components and 360 lag-action components above |5|; these remain legitimate recorded MPC history. These are input counts, not an assertion that they occupy the same rows as current out-of-range targets. Detailed per-dataset/joint counts and all hashes are in [saved assessment](E:/codex-artifacts/sonic23_teleop_resume_20260911/bfm_actor_direct_distillation_feasibility_v1/report.json).

The trainable architecture definition and inference tensor names are available. The currently executed BFMZeroInference is a functional inference-only reconstruction: its actor/backward methods use inference_mode and weights are plain tensors in a dict, not registered trainable Parameters. A future supervised actor-only fit would need an explicitly reviewed gradient-enabled module that preserves the frozen backward map, normalization and numerical output contract, plus exact initialization/export parity. The inspected upstream source subset lacks base_model.py, pytree_utils.py and agent training implementations; the local checkpoint subset has no original optimizer or replay state. The saved config/train_status is descriptive metadata, not a resumable training checkpoint. This does not prevent a new supervised fit, but it would not resume the original BFM training run.

Conclusion: local weights and all nominal actor inputs make an actor-only supervised experiment technically possible and allow direct use of the richer history. Exact distillation of all expert commands under the unchanged tanh/scaling contract is impossible for the identified88 rows. Resolve that output-range contract explicitly before selecting such an experiment; do not treat more capacity/history as evidence it will stabilize closed-loop motion. No new labels, inference, fitting, physics, downloads or architecture changes were performed. The current3054 physical-branch and conditional75000 route is unchanged.
