Prepared only. Training is owned elsewhere; this package has no witness/evaluation binding or execution authorization yet.

Reviewed runtime is now copied to source_snapshot_v1: exactly25 declared .py files, no copied __pycache__. source_freeze.json binds source-preparation review3fef12f2... . Controller, raw combined-action history, strict2ms gates,1569-control lifecycle and conditional250-control continuous hold remain unchanged. The existing failure/full291/session-input evidence remains intact.

When root selects the witness after ordinary75000 fit/export and independent evidence review, create an actual witness review configuration containing root_selected=true, ordinary_final_step75000, stage=witness, requested_main_controls0, conditional_hold_controls0, separate_head_calls1, hardware_authorized=false. selected_subject_sha256 must name exact existing head, checkpoint, fit_report and training_manifest hashes. The four review roles dataset/fit/export/source require existing absolute paths and their exact true boolean pass fields. Dataset review must bind original centers plus the physical manifest/report/collection request. No empty hashes or stand-in model files are accepted.

Then run freeze_final_package.py --mode witness --reviews <actual configuration>. This validates completed numerical fields and every source/input/review hash, writes one new witness_binding.json, and performs no inference. prepare_bound_launcher.py --mode witness generates concrete guarded launchers only after the binding validates. Submit actual binding/launcher hashes for final reviewer clearance; launch exactly once only after root selection and that clearance.

The witness uses exactly one WSL ORT1.23.2 CPU sequential, threads1, batch1 head call on fixed centers row2038/control250/frame261. It performs no BFM or physics calls. Returned data, attempted count and failures remain recorded. Its output must pass exact canonical first-head output parity later.

After witness success and root selects the canonical evaluation, create a separately selected evaluation configuration with stage=evaluation, requested_main_controls1569, conditional_hold_controls250, separate_head_calls0 and the same actual final subject hashes. Run freeze_final_package.py --mode evaluation --reviews <actual configuration>, then prepare_bound_launcher.py --mode evaluation. This additionally binds the real completed witness/receipt and writes frozen_inputs_v2.json. Final concrete source/binding/launcher review remains mandatory before launch.

Both launchers reject existing attempt outputs and use CreateNew locks; the durable child uses a native process handle acquired before WaitForExit, explicit unavailable-exit checks, input hash checks before and after execution, and nonzero verdicts for incomplete/failed canonical runs. No retry or checkpoint selection. Mutable progress readers must use ReadWrite|Delete sharing.

Seven pure/stub preparation tests pass; allfour rendered PowerShell launcher templates parse. Test templates are deliberately unbound, reside only in launcher_template_checks, and were never executed. Actual binding files/process directories remain absent.
