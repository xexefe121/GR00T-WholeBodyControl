# Fixed causal response-balanced continuation

Source preparation only. No task model, gradient, optimizer or native calls are authorized by this artifact. Actual fitting requires completed causal canonical semantics, exact source/input binding, independent review, and root concrete clearance.

Continue the ordinary causal 68000 checkpoint `10d238198e2d7826c35eaa9bd4a13ff2e05050a7f601c1f212b585c4a90eaefd` once for 3000 updates, producing ordinary 71000. Restore all six learned 1323-input actor tensors, all six AdamW moment/step states at 3000, complete RNG, normalization, span and target contract. No expansion, zeroing, fresh optimizer, calibration, new samples or labels. The first 3000 saved 864-pair schedule rows are reused exactly. AdamW ends at 6000. Root selects inclusive cosine LR 1e-5 to 1e-6, weight decay 1e-5, gradient clipping 10. The mathematical proposal's alternative LR 1e-6 to 1e-7 remains preserved and unselected.

The only loss change is the independently reviewed finite-feedback group weighting. For each original dataset/phase/group cell, multiply its original normalized endpoint-minus-center MSE by `mean(E_g)/E_g`, using immutable teacher-only zero-response energies. Groups run fastest within nine dataset/phase cells. Average all 54 weighted cells; retain coefficient 1.8188207859141674. Nominal and physical objectives, float64 cast-before-subtraction, differentiable nominal-center reuse, requested-cell denominators and all data remain unchanged. No radius division or common-unit Jacobian interpretation is introduced.

Reuse the completed shared nominal/center/physical context arrays, context chronology proof, normalization and row identities from `direct_target_causal_context_study_v2`. Physical context retains inverse actually applied prior plus once-advanced incoming history. Full-state perturbations retain their center's incoming context. No context moments or labels are regenerated. Existing pure loaders may validate immutable input arrays at actual execution.

Training keeps the reviewed split contiguous 1000+323 first layer. Export keeps the same-weight monolithic float64 normalization/linear/ELU implementation, final normalized output float32, then runtime default_float64 + existing_span_float32_promoted_to_float64 * output_float32_promoted_to_float64 and native float64 clamp. All five fixed corpus diagnostics remain: initial GPU32, final GPU32, CPU64, GPU64 and ORT64. Save both original and balanced full-state objectives, all original and weighted 54 cells, original/weighted total objectives and existing odd/even/clipping evidence. Initial outputs compare all 367570 rows against source causal final_GPU32 with identical corpus partitions. The original 1e-5 rad initial and final preclamp numerical gates remain mandatory; exact byte agreement is reported.

Fixed budgets: 9000 training forwards, 44,058,000 training rows, 3000 optimizer calls; four Torch diagnostic passes of 367570 rows /1437 calls each, one ORT diagnostic pass of 367570 rows /1437 calls; zero calibration, export tracing, BFM or native calls. Save the ordinary endpoint before export qualification. Any failure preserves returned outputs, loss/sample prefixes, optimizer attempt/return/synchronization status, all six actual optimizer step counters and final identity verification outcome. No retry, alternate endpoint or checkpoint selection.

This is one engineering trial. It cannot establish a causal advantage over extra epochs or optimizer warming. Strict full 1569-control plus conditional 250-control hold acceptance remains separate; no runtime trial is selected here.
