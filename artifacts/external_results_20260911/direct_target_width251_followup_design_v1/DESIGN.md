Source planning only. No new data, training, model call or simulation selected by this note.

The pending recovery begins at actual width81000 precontrol251. First compare its saved optimized target against both the student's actual target251 and the saved fixed-map target251. Require byte identity of qpos30/qvel29, incoming prior23/history300 and control251. Report all23 per-joint differences, RMSE and maximum absolute difference. Distinguish a saved proposal from an actually applied target by checking the branch trace. The first optimized target alone is not a successful recovery or an admitted training label.

If the single selected branch completes, independently replay the recorded main and continuous hold with the existing physical auditor. Apply the original full-source, original29 intent, return and both quiet-window gates. The actual prefix0..250 remains original provenance, including the one learned action250. It is not new expert supervision. A failure remains one prespecified failed query; do not search for another initial state to turn it into a success.

Only after full qualification, a future collection could use controls251..1268 (1018 newly executed expert controls). Preserve qpos/qvel, all incoming history, previous applied-target inverse, source frame, full291 and original optimized plan/gains. No BFM or head inference is necessary to create the direct1000 current features: qualified DirectFeatures depends only on saved measured state and original goal arrays. Append saved prior23 plus named incoming history300 in the existing sorted field order to obtain1323. Keep original coordinate convention, eight goal offsets, ground-truth root disclosure, frozen normalization and same source timing. Do not copy subsequent teacher state into the simulated plant.

For each row the direct target is the actual native-bounded applied expert target. Its stored plan map is local feedback from the connected expert continuation, not the derivative of a new MPC solve at every perturbed state. Exactly one first state251 is a fresh student-state query; subsequent controls follow the expert. Any later perturbation labels must disclose whether they use a fixed map or a separately selected fresh replan.

Before choosing a training objective, compare first-command discrepancy, phase errors, new feature support and existing nominal-fit error using saved artifacts. Preserve old nominal and response benchmarks; adding one recovered trajectory does not prove coverage of all failing student states or qualification of PICO/walk002/held-outwalk008. No optimizer duration, mixture weight or second recovery state is selected here.
