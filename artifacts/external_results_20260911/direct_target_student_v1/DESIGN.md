Preparation only. Root selected this design for source and synthetic-test implementation; no fitting, real-model inference, witness, physics, or hardware launch is authorized yet.

The new head takes exactly the retained original indices [0:52] followed by [75:1023], producing 1000 float32 inputs. Previous target, raw previous action, and BFM base features are absent. Architecture is 1000→256 ELU→256 ELU→23 linear, with default PyTorch initialization under fresh seed20260911. No zeroed last layer, tanh, BFM residual, restored weights, or restored optimizer.

Nominal data contain9904 qualified actual target rows in fixed dataset order old/query1/query250/PICO/walk002. The first three datasets each have acquisition/source/return counts100/819/100; PICO100/5780/100; walk002100/667/100. Each of the15 cells has equal total weight. No standing, held-out walk008, dropped difficult rows, target averaging, or newly generated labels enter this fit.

Root resolved the span contract: preserve the exact existing joint_span float32 array and promote it to float64. Do not recompute an unrounded difference of native bounds. Preserve teacher targets and default positions as float64. Nominal normalized labels are ((target_f64−default_f64)/promoted_existing_span_f64).astype(float32). Runtime output is default_f64 + promoted_existing_span_f64 * head_f32.astype(float64), followed by the original exact float64 native clamp. ONNX embeds only float32 feature normalization and the MLP, returning normalized_target float32[23]; it does not multiply by span or add a BFM base.

Fresh feature normalization uses all9904 nominal retained inputs, with equal15-cell population weight. Compute the global mean in float64, then the mean of each cell's mean squared deviation from that global mean. This includes between-cell differences. Floor the resulting standard deviation at0.05, then cast mean and standard deviation to float32. Probes and physical branches do not contribute to normalization statistics.

The objective is Lnominal + Lvelocity + Lphysical, each coefficient1. Lnominal is the mean of15 cell means of squared float32 normalized-target error over rows and23 joints. The two finite-response terms use normalized absolute output differences, promoted to float64, minus (teacher_endpoint_f64−teacher_center_f64)/span_f64. Default and span cancel algebraically on the student side; there is no BFM term or division by perturbation size. Both predictions retain gradients.

Velocity supervision uses the exact5000×576 saved center IDs and axes from the completed75000 fit. There are64 symmetric pairs per each of the original nine dataset×phase cells. Average23 joints, both signs, all64 pairs, then the nine cells. Reuse the corresponding differentiable outputs from the full nominal batch through an explicit dataset/control center mapping. No newly sampled IDs or RNG calls substitute this schedule.

Physical response supervision uses all3054 qualified one-step endpoints, each paired with the matching nominal successor. Use the original nine requested-cell denominators99/819/100 repeated three times and23 joints, then average the nine cells. The successor mapping is checked by explicit dataset/control identity. No BFM/base arrays or stored residual targets are used in either response loss.

Training is exactly5000 AdamW updates, lr cosine3e-4→3e-5 inclusive, weight_decay1e-5, gradient norm clip10. Each update evaluates9904 nominal +1152 velocity +3054 physical rows, totaling70,550,000 training row presentations. No intermediate rollout, early stop, best-checkpoint choice, or fit retry. Save every replayed sampler ID/axis, loss, cell loss, preclip gradient norm, LR, and attempted/returned update counters. Save the ordinary-final PT, optimizer, CPU/CUDA/Python/NumPy RNG, normalization, cast metadata and training arrays before final export diagnostics.

Proposed bounded diagnostics: one initial GPU pass and one final GPU pass over the complete153580-row corpus (9904 nominal,140622 velocity,3054 physical), plus one final CPU and one final CPU-ORT pass on identical batch256 partitions. This is460740 diagnostic Torch rows and601 ORT calls. Save all normalized outputs and report nominal15-cell, velocity9-cell and physical9-cell errors, first24 phase windows, per-joint physical target errors and clipping counts. The sole numerical export gate is maximum CPU/GPU/ORT disagreement of at most1e-5 radians after float64 reconstruction, measured before native clamping. Metrics do not select a checkpoint or claim connected stability.

GPU runtime is the isolated root-prepared PyTorch2.10.0 cu128 environment on E. Require deterministic algorithms, float32 model/parameters, no TF32, no AMP, CUBLAS_WORKSPACE_CONFIG=:4096:8, fixed CPU/CUDA seeds and explicit AdamW foreach=False/fused=False. Runtime identity and final source/input hashes must be frozen before a future selected launch. Unknown runtime, missing clearance, changed hashes, nonfinite values, failed parity, or incomplete output preserve failure evidence and stop that stage.
