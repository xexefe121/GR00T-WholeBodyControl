# D3 saved-fit auditor — source preparation

This derivative checks the single selected 10,000-update warm continuation from ordinary 81,000 to 91,000, with AdamW age 16,000 to 26,000 and D3 coefficient 0.2. Actual execution requires a completed fit, completion owner, independent source review, and concrete audit selection. No actual audit request or output is created by source preparation.

`audit_saved_warm.py` derives the completed width-81,000 auditor. `main_derivation.patch` records the changes. Old nominal, full58, physical, context, balancing, graph, restoration-comparison helpers stay byte-exact. `audit_width_math.py` remains as the source of the unchanged ramp/cosine rate function; its old expansion functions are never called by the new main. Old width-specific tests remain preserved references and are not the selected D3 test suite.

New `audit_recovery_math.py` independently checks every full-width actor/moment/group/RNG against the qualified source; no expansion or model construction occurs. It reconstructs the 1,018 row identities, phase counts 99/819/100, incoming prior/history suffixes, connected plan indices, all three D3 phase metrics, and the D3 training ledger. D3 float32 squares are reduced independently in float64 with the existing nominal 3e-7 relative reduction tolerance. The weighted training contribution uses the declared float32 scalar product; its addition to the old objective uses float64. The original N15/P9/F54 reductions are unchanged.

Five backends each retain four corpora: 9,904 nominal, 354,612 full-state endpoints, 3,054 physical, and 1,018 recovery rows. Each backend has 1,441 calls and 368,588 rows. The audit checks all 7,205 saved diagnostic call records, 20 prediction arrays, 12 preclamp FP64 comparisons at the original 1e-5 rad gate, and 12 final-GPU32 drift arrays with clipping counts. Initialization retains the original three-corpus 1e-5 gate; the new recovery corpus has a saved initial baseline, without an invented prior byte gate.

Training accounting requires 40,000 returned/verified forwards and 157,040,000 rows, exactly 10,000 updates, the unchanged saved schedule and 250-step inclusive ramp followed by 9,750-step inclusive cosine. Original six-column progress and original/weighted54-cell arrays remain distinct from the new three-column D3 objective and three phase-cell arrays.

`audit_recovery_bindings.py` binds actual collection, qualification, source review, normalization, consistency and warm-restoration review by path and hash. It does not recompute feature construction or claim that the connected expert states are 1,018 independent fresh student queries. The source qualification states one fresh query and 1,017 subsequent connected expert states.

Four audit execution helpers reuse the established captured-handle hidden PowerShell supervisor. Only audit kind, fit namespace, and selected endpoint literals change. The saved-audit completion verifier is byte-identical to the prior verified helper. Final fit-owner launch/dispatch/absence schema will be pinned before this source is frozen.

Selected synthetic suite: `test_balanced_audit.py`, `test_nominal_reduction.py`, `test_rule_metadata.py`, `test_prior_math.py`, and `test_recovery_audit.py`. Tests use synthetic arrays/tensors and static source checks only, no model forwards or optimizer updates. Execution is deferred until the parent releases the clock timing window. Actual task arrays/checkpoints remain unread until the completed-fit audit is separately selected.

Limit: saved evidence checks provenance, initialization, arithmetic, and counters. It does not independently rerun model outputs, optimizer gradients, physics, or establish closed-loop stability.
