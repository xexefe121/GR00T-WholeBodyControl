Saved causal71000 runtime semantics and fixed-map audit preparation

Core audit_saved.py, context_math.py, fixed_maps.py and test_saved.py remain byte-identical to the qualified causal68000 audit. All common mathematical functions remain AST-identical. Incoming prior23 plus pre-update sorted named history300, current1000 features, applied-target inverse, once-only history advance, original startup/terminal BFM feedback, source/control/native clocks, first-failure capsules, and conditional continuous hold preserve their original checks.

New release checks consume the actual ordinary71000 warm single-fit16 roles, retained optimizer3000→6000 protocol, selected six group weights, separate source/helper reviews, and completed training audit owner. Producer weight metadata uses its full literal name; energy provenance uses Emean/Eg. Root intent is independently bound to the same main trace and root physics report; false behavioral verdicts are retained without blocking a successful evidence audit.

The future request helper requires completed canonical owner, source review, exact root physics and intent reports. A separate concrete root review of the resulting request and launcher remains required. Source preparation reads no actual71000 trace or failure arrays. The audit may later read saved evidence only; it never runs a model, optimizer, native step, or planner.

All issued learned controls are checked. Existing unique same-clock query250 maps retain the original58-axis difference, full gain matrix, correction ±0.1 clamp, and native target clamp. Missing map coverage is explicit. Map results are stale committed-map comparisons, not replanned expert commands or a stability proof.

The durable wrapper retains captured hidden child handles, CreateNew attempt records, known raw exit accounting, shared-delete .NET streamed hashes, and no automatic retry. Completed evidence remains separate from controller qualification. Old audit receipts and failed attempts are preserved.

Synthetic test setup initially used an environment without SciPy and stopped during collection; no tests or task work ran in that attempt. Final synthetic evidence uses the existing Windows Python with NumPy/SciPy. Actual saved-map audit still requires the previously qualified WSL NumPy1.26.4 environment.
