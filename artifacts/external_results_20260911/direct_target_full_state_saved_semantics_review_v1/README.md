Source preparation for one saved evidence audit of ordinary65000.

The audit imports NumPy and SciPy only. It extracts the qualified pure state and
DirectFeatures expressions by AST; it never imports the evaluator, MuJoCo, Torch,
ORT or a controller. It checks the actual main command prefix, optional rejected
precontrol or strict failure capsule, and any continuous terminal hold. The
history update uses the previous committed command. Learned feedback uses the
inverse applied target; startup and terminal preserve the original raw BFM action.

The future request requires the completed owner and independent physics report
for every recorded segment, plus a positive source review matching every source
hash. It pins actual sources and all directly consumed release/runtime receipts;
the full immutable launch/postrun maps and completed owner establish the wider
runtime inventory identity without traversing unrelated training dependencies.
No actual request exists during preparation. The actual audit creates results_v1
once and preserves its checks and active failure if anything fails; no retry.

Every issued learned row also receives a same-clock frozen query250 map comparison
and six-group preclip tangent decomposition. The full gain product and nested
feedback/native clamps are unchanged. These stale maps are not replanned expert
truth, feasibility certificates, or a remedy for physical failure. Reported group
norms can cancel and are not additive attribution percentages. Direct1000 does
not use action/history as head inputs; those remain BFM handoff bookkeeping.

Synthetic tests exercise finite-array arithmetic, partial failures, phase
boundaries, held terminal source clocks, prior/history ownership, alias-free path
binding, full58 grouping and import boundaries. No task model/native calls.
