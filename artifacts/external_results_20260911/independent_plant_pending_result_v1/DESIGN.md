# Pending worker result preparation

The preceding saved run recovered all four plant job BUSY events but lost result 656 after a single BUSY return with 17.413713 ms remaining. Its 15 captured-step timing misses remain independent failures. This package is source and deterministic fake tests only. It contains no selected request, launcher, runtime installation, actual worker, serialization witness, model call, or native step.

The plant now adds its literal `epoch_ns + activation * 10 * 2_000_000` deadline to each Job identity. The worker reads that identity field; it never infers a deadline from `created_ns + 20 ms`, and Binding.epoch remains a run identity. Plant admission independently recomputes the deadline and checks the full issued identity. The authoritative received timestamp remains after plant decoding/copy/validation; late publication cannot become timely admission.

The worker retains explicit state across its existing 1 ms loop. It builds one immutable result, preserving the original pre-construction `completed_ns` and adding a separate `payload_ready_ns`. Only a fully recorded, unambiguous BUSY return permits another attempt. There is at most one publish per worker iteration and at most 20 attempts total, all starting before the original deadline. A BUSY or PUBLISHED return after the deadline is terminal. FULL, CORRUPT, other non-BUSY errors, ambiguous exceptions, logger failures, and invalid clocks stop the worker without retry. There is no recomputation, new command, new history update, deadline extension, busy loop, or plant wait.

The shared mailbox still performs one nonblocking call per fixed slot and is byte-preserved. Its single poll can return two TAKEN jobs. Those already-consumed immutable publications are retained in a fixed two-item batch in original slot order. The worker processes only one job at a time, does not poll while a result or batch remains, and does not compute the next buffered job until the prior result was published and logged successfully. Duplicate, invalid, or expired jobs fail closed. A stop with pending or buffered work preserves that work and fails the worker verdict.

Attempted, returned, published, ready-event, publication-event, and terminal-event counters are separate. Known statuses are copied before requesting a post-call timestamp. If ObservedEndpoint throws after a shared write, the worker records an attempted call with unknown return; any available transport overflow evidence remains separate. The first failure and terminal descriptor survive logger failure in owned memory. Process teardown and native exit serialization remain under the unchanged existing supervisor; an outer hard kill can still prevent final worker output.

NativeStepper, full291/packed373 capture, manual PD, native oracle, warning limits, history update, epoch, control phases, outer plant loop, shared mailbox, and worker lifecycle are unchanged. The dormant runner's import inventory adds only pending_result. Existing tests retain direct late-result injection to test plant rejection independently of the new worker expiry behavior.

The fixed worker event ledger remains 125,000 slots. Valid traffic has a bounded number of result attempts; pathological repeated poll BUSY events can still exhaust the ledger, which is a preserved failure rather than evidence of successful completion. Additional state/log allocation may change measured runtime. No improvement in plant deadline timing or successful full-scope balance follows from these tests.

Before any future actual run, prepare a new request with this explicit retry/deadline schema and a matching independent saved audit. The previous worker audit assumes one result publication and must not be used to qualify this ledger. Original 18,190-step scope, four MJB serializations, 2 ms/20 ms deadlines, debt/elapsed limits, and watchdog budgets must remain literal. No new run is selected here.
