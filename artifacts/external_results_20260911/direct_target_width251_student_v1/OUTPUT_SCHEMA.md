# Conditional integration candidate

No update count, learning rate, recovery coefficient, fit request or launch is selected by this source draft. The root will freeze a literal protocol and ordinary endpoint after qualified data and consistency evidence exist. `recovery_protocol.Protocol` is constructed only inside the admitted entry point, never from files at module import.

The candidate keeps all 28 old width81000 source modules byte-identical. New entry is `train_recovery.py`; old `train_response_balanced.py` is retained as an unselected reference. `warm512_restore.py` is the separately root-reviewed exact adapter. The unchanged original `WiderContextTarget` is reused; no expansion or moment reset occurs.

The candidate consumes the existing complete 10,000 x 864 response schedule with its original first3,000 proof. A future selected positive update count may use at most its full prefix, without wrapping, regeneration, or schedule selection from results. Learning rates are an explicit immutable float list, one entry per selected update. Future root preparation may replace that list with a reviewed literal schedule source while preserving its exact selected values. No learning rate or new recovery coefficient has a default here.

Each selected update has four counted forwards: all9,904 nominal rows,1,728 full58 endpoints, all3,054 physical endpoints, and all1,018 recovery rows. Total15,704 rows and four calls per update. Original15 nominal,54 response and9 physical cells and fixed response coefficient/group weights are unchanged. The new term is a separate three-phase mean of original float32 normalized-target MSE, phase counts99/819/100, with a future positive coefficient. The same single backward, clip10 and AdamW step acts on the combined loss.

The old six `training_progress.npy` columns remain nominal,balanced_full_state,physical,old_balanced_total,learning_rate,actual_combined_preclip_gradient_norm. Old original/weighted cell arrays remain unchanged. New `recovery_objectives.npy` has recovery_MSE,weighted_recovery,combined_total columns and `recovery_cell_losses.npy` retains all three phase values. Both are saved at ordinary progress checkpoints and on failure.

Source initialization records full six actor tensors, all AdamW states/groups at16000, exact RNG and frozen normalization, with `expansion_performed=false`. Source ordinary81000 initial prediction gate still compares only the original three corpora, with identical partitions and original1e-5 preclip tolerance; byte equality is reported. The new initial recovery corpus creates a separately retained baseline, not a comparison to predictions at other physical states.

Each of initial_GPU32,final_GPU32,CPU64,GPU64,ORT64 retains all three old corpora in their original order and partitions, then adds recovery1018 in four batches. Per backend:1,441 calls,368,588 rows. Four Torch backends total5,764 calls/1,474,352 rows; ORT1,441 calls/368,588 rows. Nine original FP64 parity comparisons remain and three recovery comparisons are added at the same1e-5 gate. The promoted architecture, exact float32-to-float64 weights and manual graph arithmetic are unchanged; only ordinary-endpoint admission and graph label are generalized. Drift saves the original nine arrays plus three recovery arrays, including existing clipping counts.

All old metrics are computed by unchanged `summarize_balanced`; recovery phase metrics/first24 errors and combined objective are appended. No benchmark weighting is relabeled as the new objective. The source checkpoint remains an exact required subject; final ordinary/optimizer endpoints are81000+U/16000+U.

The enclosing concrete gate retains old data/runtime/source/owner/audit checks and adds exact completed collector/request/source/qualification, consistency report and warm-source review bindings. Consistency completion is evidence availability; the root's concrete fit selection must explicitly acknowledge that evidence. No conflict is automatically repaired or averaged. `shared/recovery_rows.npz` copies the qualified collection bytes, while all old context and normalization files remain unchanged.

No actual request, frozen fit packet, launcher, checkpoint, corpus load, model forward or native call has been created by this source integration. Full numerical execution remains unselected. Legacy copied tests tied to old fixed budgets are not treated as tests of the new candidate; dedicated synthetic/static tests are supplied separately.
