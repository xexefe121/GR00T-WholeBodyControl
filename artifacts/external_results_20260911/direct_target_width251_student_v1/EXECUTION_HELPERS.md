# Qualified recovery fit packet

The immutable 39 trainer files remain unchanged. `prepare_execution.py` reads the exact root-selected protocol (10,000 updates, explicit 250-ramp/9,750-cosine rate list, recovery coefficient 0.2) and source review. `prove` uses JSON only; `freeze` performs the complete file rehash and creates the actual request, source snapshot, frozen receipt and launch receipt exactly once. It never writes an approved clearance or starts training.

The candidate's `root_selected=true` identifies the selected configuration. Execution still requires a separately written, approved `training_clearance.json` bound to a concrete review. That review must expose `prelaunch_review_pass=true`, `training_request_sha256`, `frozen_receipt_sha256`, `launcher_sha256`, and `launch_receipt_sha256`. Clearance must expose those identities (`request_sha256` for the request), review path/hash/pass field, launcher path/hash, causal condition, all selected update/end-step fields, and `automatic_retry=false`.

`dispatch_fit.ps1` and `run_fit_durable_v1.ps1` both require literal `-ClearanceSha256` and `-LaunchReceiptSha256`. Dispatch uses hidden Windows PowerShell 5.1 with `-NoProfile -NonInteractive`, records the exact arguments and captured wrapper handle, and returns its PID. The wrapper retains the prior qualified CreateNew lock/start/child/exit, hidden isolated Python child, captured child handle before wait, shared-delete .NET hashing and known raw-exit behavior. Unknown exit cannot pass. No retry is provided.

The launch receipt lists the exact frozen inputs, 39 snapshot sources, request and frozen receipt. The actual pre/post pin map additionally includes the launch receipt itself, actual clearance and concrete review. The owner reconstructs this full map and checks every entry. It binds dispatch attempt, actual dispatch/start/child/exit, logs and an independently observed process-absence receipt. `owner_completion_verification.json` separates `accounting_passed`, `completion_passed` and `numerical_completion_passed`; no owner result qualifies physical behavior.

Owner keeps the original direct subjects and adds `recovery_rows`, `collection_report`, `collection_request`, `collection_qualification`, `collection_source_review`, `consistency_report`, `warm_restore_review`, `selected_protocol` and generated `recovery_evidence`. Each has a digest in `direct_subject_sha256` and a literal path/digest descriptor in `direct_subjects`. Process records also appear in `output_sha256`.

Own width512 `fit/shared` paths supply normalization, context, manifest, all 10,000 response-schedule rows, and retained 3,000-row source schedules. The unchanged trainer copy rule prefixes those retained filenames once more, producing `source_source_schedule_centers.npy` and `source_source_schedule_axes.npy` in the new shared output; full schedules remain `schedule_centers.npy` and `schedule_axes.npy`. Auditors should follow actual copied filenames and saved `reused_inputs.json`, without assuming an older source namespace.

`metadata_schema_proof.json` checks real saved JSON identities and manifest members without loading checkpoints or arrays. `metadata_gate_proof.json` executes only the two AST-extracted actual recovery JSON gate functions. Its recovery-row digest is declared, not rehashed; complete rehash is mandatory during `freeze`. No binary-read claim is made by those lightweight proofs.
