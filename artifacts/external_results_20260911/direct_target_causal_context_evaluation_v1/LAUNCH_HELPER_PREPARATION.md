The eight additive launch helpers prepare one explicitly selected ordinary68000 endpoint. `--condition blinded` or `--condition causal` is mandatory when a future actual review configuration is created. Neither condition is selected by this source package.

The runtime remains the independently reviewed 36-file source directory. Its public input contains 1323 float32 features, and its output remains 23 normalized float32 targets after the qualified FP64 export. The original 1569-control lifecycle, conditional continuous 250-control hold, native limits and all per-step checks remain unchanged. A fresh condition-specific one-call WSL witness must pass before canonical activation. Startup and terminal BFM behavior remain unchanged; the learned phase makes no BFM calls.

Future actual inputs come from `direct_target_causal_context_study_v1`: paired report under `fit/`, shared normalization/context alignment/manifest under `fit/shared/`, and each condition's report/checkpoint/head/manifest under `fit/{condition}/`. The coefficient comes from the training request's existing `coefficient_source` subject. Both completed condition reports, all 18 role identities, positive independent fit audit, final release review and condition-specific owner are mandatory. No future hashes are invented.

The authoritative owner aliases will be the study package's frozen `verify_completed_v2.py` outputs: `blinded_owner_completion_verification.json` and `causal_owner_completion_verification.json`. The local `prepare_owner_views.py` and its tests are preserved unexecuted draft alternatives; they are excluded from the actual eight-helper plan and will not emit duplicate owners.

The inherited runtime inventory includes the actual WSL libraries, model, native inputs, reference, canonical fixture and reviewed sources. It does not recursively rehash the unused training corpora. Immutable fit/data/export qualification receipts carry that lineage. Generated launchers retain the original hidden process, captured child handle before wait, CreateNew lock, raw-exit preservation, nonzero incomplete/failed diagnostic status and exact pre/post input hashes. Initial WSL working directory is `/` so the pinned bootstrap can mount E first. PowerShell path comparison uses explicit character codes 92 and 47.

Source/stub preparation only: no actual review configuration, binding, activation witness, controller inference, native step, training or clock rerun is performed here. A failed or partial physical run remains a failed diagnostic even when Python exits zero; independent native/intent audits remain necessary for qualification.
