Independent saved-only checker for the matched3000 context pair. No task model, ORT, gradient, optimizer or native execution occurs in this package. Three qualified math/tree files remain byte-identical; the graph checker changes only1000→1323 shape literals. New context math reconstructs named history and applied-command prior independently, then checks saved normalization, exact matched initialization and schedule, complete cell metrics, FP64 graph/parity, ledgers and release subjects.

The actual audit is not selected or executed by preparation. After both ordinary endpoints and the corrected owner-v2 receipts exist, root source review plus the root-selected one-audit instruction permits preparing an actual request, launch receipt and one hidden durable audit. Existing results, locks and failed attempts are never overwritten or automatically retried. `export_qualified` is a boolean; `condition_export_qualified` and `condition_subjects` preserve both endpoints without selecting a controller.

An incomplete producer pair cannot qualify for this completed-pair audit. Its original condition/pair failure evidence remains preserved. Any audit mismatch writes its own stage, tracked input hashes and check prefix and exits nonzero. Saved prediction identities and algebra are verified; unsaved gradients and model authenticity are not recomputed.
