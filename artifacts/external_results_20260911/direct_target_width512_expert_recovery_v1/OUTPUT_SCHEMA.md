Future execution remains unselected. The source package defines one fixed query at actual precontrol251 and one connected expert continuation.

`inputs/precontrol251.npz` contains integration float64[291], spec int64 scalar8191, qpos float64[30], qvel float64[29], history_flat float32[300], previous_action float32[23], five named history buffers, warning counts/lastinfo int32[8], recorded_controls int64 scalar251 and actual/expected float64 time. `inputs/actual_prefix251.npz` preserves251 controls,252 control-state boundaries,2510 force/metric rows and2511 physics boundaries. Original full trace remains separately pinned. `inputs/selection_receipt.json` binds the prespecified trace, completed semantics and extraction request; it exposes exact two output hashes.

`initial_restore_preflight.json` qualifies restored full291, separately restored warnings, original repeated clock and all251 incoming contexts. It reports zero actor/optimizer/native steps at this boundary. Batch construction and nonintegrating native setup are separate work. `initial_seed/{request.json,report.json,selected.npz}` retains the first seed search/certification; selected.npz is absent if no seed qualifies. Initial H30 feasibility alone does not admit labels or claim successful full recovery.

`nominal/trace.npz` and trace.partial.npz retain the original oracle schema with exact actual prefix. The `initial_integration` belongs to global0; `branch_initial_integration` is pre251. `controller_mode=1` denotes learned250 in the prefix and fresh expert only where global_control>=251. `nominal/branch_only.npz` begins at savedpre251, never includes copied prefix as labels. `nominal/plans/plan_00251.npz` and subsequent every-five-control plans retain states/targets/gains. First actual expert action may be compared to the old learned action only as a new student-state query; later branch states are expert-induced.

`nominal/report.json` exposes full_segment_completed, actual_expert_controls masked byglobal_control>=251, newly_executed_controls, newly_executed_native_steps, preserved_prefix_controls251, BFM_prefix_controls250 and learned_prefix_controls1. Inherited source/return/standing metrics cover the full original lifecycle. `post_lifecycle_hold_5s` runs only after all1569 combined controls complete; it preserves continuation time/state and contains250 requested controls. `outcome.json` keeps both results and labels_admissible=false pending independent complete physical and intent audits.

`nominal/first_expert_target.npz` saves the first optimized, corrected/clamped target at251 before imminent preview, with the existing nominal target/correction, actual state and incoming prior/history. This is a proposal until the actual trace records its execution. Later saved-only comparison against the previous same-clock fixed-map target needs no additional expert query or model call.

`work_counters.json` and append-only `work_snapshots.jsonl` expose phase, per-category attempted_calls/returned_calls/attempted_units/returned_units, fixed ceilings, active call stack, first wrapped failure and first budget refusal. The private aggregate is the sum of four disjoint private-native categories. High-level seed rejections may produce attempted>returned without fatal branch failure; inspect the actual outcome and returned primitive counts. Batch partial-call internal progress is explicitly unknown. A killed process without final task_ended evidence has incomplete accounting.

`last_actual_state.npz` attempts a final full291, qpos/qvel, actual qfrc_actuator, applied forces, warnings, incoming controller memory and control count, with per-field capture errors in last_actual_state_capture.json. Capture never steps, forwards, repairs or replaces the actual plant. `failure.json` preserves uncaught runtime faults; original planner and branch-level rejected seeds/failures remain in their qualified reports. `driver_completion.json` separates normal driver return from requested-branch completion. The future durable wrapper must preserve raw process exit separately from physical/intent qualification; this source package has no actual launcher or passing completion claim.
