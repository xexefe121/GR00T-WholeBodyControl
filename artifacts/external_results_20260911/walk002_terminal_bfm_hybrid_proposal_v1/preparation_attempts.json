{
  "kind": "read_only_preparation_corrections",
  "physics_inference_optimizer_executed": false,
  "attempts": [
    {"issue": "Assumed timeline phase key return_to_standing", "outcome": "KeyError before any artifact output", "correction": "Read exact timeline key return_ramp; assert terminal250 plus proof margin50"},
    {"issue": "Independent vector gravity signed zeros differ from canonical matrix-vector expression", "outcome": "All numeric values equal, eight float32 signed-zero history components differ", "correction": "Retain independent numeric check and add exact frozen observation expression byte check for all1118 rows; disclose difference, no tolerance"}
  ]
}
