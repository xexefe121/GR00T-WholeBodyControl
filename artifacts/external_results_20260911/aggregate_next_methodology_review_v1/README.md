The authorized original-BFM entry250 baseline is the best immediate discriminator. Keep every training candidate frozen while it runs. Terminal yaw4 standing is not evidence that original yaw2/h8pos1 BFM is safe from canonical initialization; existing matched zero-head evidence covers only100 controls. No new training or query is recommended before this narrow baseline and the failure diagnosis are complete.

The aggregate failure begins from a training input, not an initially unseen history. At control0, features, BFM base, previous action, all300 history values, sensed state, qpos and qvel equal original teacher row0 exactly. The deployed command has0.0218575rad RMS error versus its known qualified target, with ONNX versus saved Torch command difference only3.28e-7rad. Native target clipping first occurs at control3; prior action first leaves combined per-joint training ranges at control4. By failed control14, nearest feature RMS distance is1.4629, and prior action reaches9.78075 versus training maximum6.69557. Root independently reproduces all147 physics steps and the right-knee20.0663rad/s violation. This establishes initial fit error followed by rapid distribution shift; it does not isolate which first-step error caused the eventual failure.

Lower learning rate: directly supported as a convergence hypothesis, not a proven controller repair. The same fixed3e-4 optimizer has final10k diagnostics fluctuating: old/new RMSE at55000 is0.02515/0.02531rad; ordinary60000 is0.03156/0.03189rad. Query1 improves to0.02000rad but known startup and early per-joint errors remain. A predeclared lower-rate continuation could test fit accuracy with unchanged2537 labels,1069 features, normalization, architecture, loss and dataset/phase weights. Preserve complete model/AdamW/RNG, declare the changed rate and fixed extra budget before starting, select ordinary final only, and report all dataset/phase and first-control errors. Do not deploy an earlier diagnostic checkpoint or choose a rate from rollout results. Lower nominal fit error may still leave an unstable policy between sampled states. Existing data does not establish an appropriate numeric error threshold for control safety.

Explicit measured300-value BFM history: a legitimate observable input, but the current failure does not prove it is missing information. The residual head sees current proprioception, received future-goal samples, previous action and23-value BFM base; it does not directly see300 history values. The BFM base is a many-to-one compression of history, so adding measured history could distinguish some contexts and predict future BFM evolution. However, control0 already matches history exactly and still has fit error. No exact input/target conflict exists in the qualified combined data; near-neighbor steep target changes also involve current-state differences and hidden MPC warm/seed state. Therefore history augmentation is a larger architecture hypothesis, behind the simpler fit and phase questions. If later authorized, use only the same named sensor/action history with exact lag/order/preclip semantics, preserve old1069 normalization, fit a separately declared normalization for the new block, and verify zero new-input weights preserve the existing function before training. No planner gains, warm targets, source ID or clock feature may substitute for measured history.

Another actual-state DAgger query: strongest way to obtain valid targets on new states; it does not fix failure to reproduce already known row0. Select a recorded safe precontrol boundary before failure under a declared rule, restore full integration/history/prior action/clock exactly, and require fresh replanning plus uninterrupted remaining source/return and quiet hold under independent gates. Never query the partial invalid state and call it a valid initial state, reuse old nearby targets, or admit only a locally feasible600ms plan. A second recovered branch adds coverage but leaves regression accuracy and hidden planner dependence entangled. A blanket repeated-query loop is not justified.

BUFFERING/STANDING mode: the existing timeline already declares initial_standing0..249, acquisition250..349, source350..1168 and return1169..1268. A supervisor can select an explicitly declared standing controller during initial packet buffering; lifecycle mode need not become a clip-ID/time feature in the neural network. If the entire250-control original BFM baseline passes strict native and quiet checks, this supplies direct evidence that MPC entry corrections are unnecessary for that standing mode. It does not qualify enabling the current head at250: BFM entry creates a new actual plant state, previous action and all300 history values. Neither original-MPC entry history nor earlier student-query history may be pasted into it. Future-goal lookahead near the entry boundary also prevents treating all250 controls as an identical static goal.

Any acquisition transition must retain the actual BFM plant/history continuously, preserve all100 ramp and819 source controls at original times, and pass full lifecycle plus terminal/hold. A fresh expert query at the actual BFM pre-acquisition state would be more relevant than another near-start query if the task adopts this mode. Inspect current-head feature coverage and command discontinuity there as diagnostics only; they cannot certify the transition. Do not enable only at source350 and skip the required acquisition ramp. Do not reset root pose/history, blend saved trajectories, add an arbitrary residual cap or silently change action-history arithmetic to make a switch pass.

Decision order: finish original-BFM250 evidence; assess its actual end state and quiet behavior. If it fails, standing-mode explanation is unsupported for this configuration. If it passes, a principled standing/acquisition split becomes a concrete candidate, with transition evidence still missing. If the unchanged all-phase residual strategy is retained instead, lower-rate ordinary-final refinement addresses the observed in-distribution fit error more directly than adding history or another branch. No additional candidate, training budget, physics run, query, or hardware action is selected or launched by this review.

Evidence scope: read-only source, recorded fit diagnostics and sibling saved-array failure diagnosis; root supplies independent physics reproduction. Input hashes bind exact sources/reports. Reviewer inference, optimization and physics counts remain zero.
