Recommend one fixed 10,000-update D3 trial with **lambda_D = 0.2**, preserving old N15, P9 and balanced F54 coefficients. Restore the complete ordinary81000 width512 actor, AdamW step16000 and RNG. Reuse the entire saved10,000×864 response schedule once, in order. Use the previous width trial's inclusive250-step linear ramp1e-6→1e-5, then inclusive9,750-step cosine1e-5→1e-6. Final ordinary/optimizer steps would be91000/26000. This is a recommendation, not a selected fit or evidence of stability.

The weight has a clear dataset meaning. N15 gives each original dataset-phase cell coefficient1/15. D3 averages three new phases, so0.2/3 gives each new phase the same1/15 coefficient. The new branch receives one dataset's nominal mass, with old nominal aggregate1, physical1, response1.8188207859141674 and six response weights unchanged. New branch mass is1/6 of the combined nominal-style mass. Lambda1 would give each new phase five times an old phase's mass; current evidence does not justify that priority. This is neither gradient balancing nor an optimal-weight estimate.

Saved evidence supports adding supervision, while limiting confidence. At actual251, fresh optimized target differs from the same-state old committed map by0.12394156rad RMS/max0.47622695. Actual student differs from that old map by0.02775804rad RMS, and from fresh target by0.13363636rad RMS. Thus better fit to the old map alone would not reproduce this fresh query. The new target was issued and its connected expert branch completed; this does not show that the student can follow the same branch after training.

Closest old row to new251 in the full normalized view is physical dataset2/control251: input RMS0.02773198, current-feature RMS0.03042808, history difference0, prior RMS0.06310861, but target RMS0.12305213/max0.47117643rad. Old candidate's feedback clipped; new target's feedback did not. These are nearby, distinct inputs and different planner histories, not an exact contradictory-label pair. A committed feedback map and fresh optimizer are different target-generating procedures. Unobserved MPC warm-start state remains a plausible explanation; these comparisons do not isolate it.

Variation extends beyond the first row. Among the fixed first24 queries per phase, full-view nearest-neighbor target-RMS medians are0.15115rad acquisition,0.24037rad source and0.15574rad return; source maximum0.59163rad. Corresponding input-RMS medians are0.15020,0.30915,0.22690. The old nearest targets should not be interpolated or averaged into the new trajectory. A larger D3 coefficient could bend the fitted function sharply between nearby labels and interfere with old response supervision. Keeping original terms visible and preserving their coefficients makes that tradeoff inspectable.

The six alias views found no duplicate groups across13,976 rows. This covers9,904 nominal +3,054 physical +1,018 new rows, not the354,612 full-state probe inputs or unobserved states. The72 proximity queries are deliberately fixed first24-per-phase samples, not a global neighborhood certificate. Only row251 is a fresh student-state query; the other1,017 are expert-induced states. No current-model predictions or gradients on those1,017 states were measured in this diagnosis.

The prior width trial gives a practical LR scale: the same ramp/cosine profile improved all15 nominal,9 physical and54 response cells, with nominal training loss peaking at only1.053× its first batch and no gradient clipping. Its final nominal loss fell69.65%, while balanced response remained1.07439× the zero-response baseline. Reusing this profile avoids simultaneously introducing a new optimizer schedule. It does not separate width, extra updates and optimizer effects from that older trial, or guarantee the same behavior after adding D3.

Preserve exact81000 initialization comparison on old corpora at1e-5rad, plus the new initial D3 baseline from the already counted fourth corpus. Four training forwards/update imply40,000 calls/157,040,000 rows; five diagnostic passes retain all368,588 rows each. No extra gradient calibration, scouting pass, alternate coefficient fit, checkpoint selection or rollout sweep. Report old and new per-phase errors, balanced/original response, clipping and combined objective at the ordinary endpoint even if some worsen. All same-weight FP64 parity, full original1569+conditional250 physical/intent gates and measured sub20ms deployed-controller requirement remain.

If the fixed endpoint cannot reduce D3 without materially worsening old nominal/response behavior, retain that interference evidence. If supervised D3 improves but the student still departs, the connected expert branch has not supplied later student-state coverage. Either outcome motivates a specific next design; neither authorizes automatically extending duration or raising lambda.

All inputs and72×4 saved-JSON proximity summaries are pinned in report.json. This analysis loaded no NPZ, NPY, checkpoint, model or native state and made no forward, gradient, optimizer or simulation calls.
