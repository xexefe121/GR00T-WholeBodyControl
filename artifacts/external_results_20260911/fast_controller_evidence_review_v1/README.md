**Command capacity and closed-loop learning both failed, in different experiments. Increasing the residual cap alone is not an evidence-backed solution.** This review reads frozen code, existing reports and saved arrays; it launches no inference, training, optimization or dynamics. `evidence_inventory.json` binds 26 existing inputs by path, size and SHA256. `independent_array_crosschecks.json` independently reproduces the capacity figures below from saved arrays and verifies all 819 source frame indices exactly.

| Existing experiment | What the command map can represent | Actual result and warranted conclusion |
|---|---|---|
| Direct MPC student, `student_3s_fit_v1` | Frozen `student_snapshot.py:102` computes `delta = linear_MLP(features) * .5`, then clips `qref + delta` only to native limits. The 0.5 factor is a scale, not a radius or tanh bound. | Teacher-state validation target RMSE 0.027263 rad; nominal replay fails at 5.840 s, 292/500 controls, before source begins at 7 s. Held-out initial-perturbation replay fails at 5.616 s, 280 full controls plus 8 physics steps. Thus an output-radius bottleneck does not explain these failures. Teacher-state fit error, ensuing state/history distribution shift and contact-sensitive teacher behavior remain entangled. |
| BFM-conditioned supervised residual, `bfm_residual_fit_3s_r025_v1` | A genuine `0.25*tanh(MLP)` cap, added to frozen BFM preclip target. | 8.1826% of short-dataset label components exceed the radius. Even a perfect capped head has 0.078796 rad **preclip residual-label** RMSE; learned validation RMSE is 0.086582 rad. Learned nominal runs all 500 controls but exceeds a native joint bound by 0.006366 rad and worsens source leg RMSE from zero-head 0.1533 to 0.2458 rad. Perturbed replay fails after 418 full controls plus 4 physics steps. Capacity limitation is proven; the fraction of closed-loop failure caused by that limitation is not isolated by a matched uncapped experiment. |
| Same capped BFM command family against new full walk003 teacher, `walk003_fixedcap_capacity_v1` | Same radius and 1069 features, evaluated at all actual teacher states/history. No fit was launched. | In all 819 source controls at least one component exceeds the cap; 40.2612% of source components do. Required residual magnitude p95/max is 0.884588/2.813692 rad. Even the best capped command, after native target clipping, has source **applied-target** RMSE 0.248806 rad: legs 0.223456, arms 0.287158; max error 2.563692 rad. This head cannot reproduce this particular teacher/prior combination. It does not prove that every successful controller must use these exact commands. |
| Separate PPO BFM residual, `train3h_v1`, final saved update 800 | `0.15*tanh` head, different 188-feature observation and measured-heading goal convention; trained on-policy without MPC expert labels. | Latest full 800-update evaluation still rejects full-body qualification: walk002 source root p95 0.3910 m; walk003 0.5964 m; PICO 0.2812 m with joint-bound excess 0.001980 rad; walk008 stops at 417/1114 attempted controls. Earlier update-200 counterfactual residual max was only 0.033827 rad versus its 0.15 cap. That early result supplies no evidence of saturation; it does not establish the final checkpoint's saturation rate. More epochs or a wider cap is not justified by these results alone. |

The direct and supervised-residual fits used only four accepted 10-second trajectories of one previously seen recording: 350 entry controls plus 150 source controls each, 2,000 samples total. Thirteen of 17 attempted perturbation/noise cases were excluded. Validation held out a perturbation of the same recording, not a motion. The collector's acceptance was completion/no failure/native range, not all original-intent gates. This is limited local physics evidence, not a qualified full-source demonstration. Direct inference was already fast (nominal p95 3.04 ms); its failure was behavioral, not deadline overrun.

The old full walk002 teacher replay is weaker still: `teacher_native323_full_perturbed_v1/report.json` contains **0 eligible cases of 17**. Nominal completion exceeds the left ankle bound by 0.001824873 rad; all eight initial perturbations and all eight observation-noise cases fail before full completion. Those are saved-plan/saved-feedback replays, not fresh replanning at actual student states. `source_phase_metrics_v2.json` supersedes a previous metric slice that included return/standing. Arbitrary states combined with saved `K` must not be relabeled as successful expert demonstrations.

New evidence does support a narrower starting point: `walk003_terminal_bfm_yaw4_hybrid_v1/recorded_source_audit_v2.json` passes all 15 recorded-source gates over all 1,569 lifecycle controls, including all 819 source controls and 15,690 native physics steps. Its first 1,269 controls are the actual MPC entry/source/return; its final 300 controls use native BFM standing, yaw gain 4. Both the original-lifecycle tail and the separately scored extension pass unchanged quiet-standing diagnostics. The original MPC tail's 7.834 rad/s peak should not become the desired standing behavior. This remains nominal offline evidence: no perturbation-qualified new teacher was found. Source joint/foot errors are against the declared native v4 reference; hand/head and root/heading gates retain original intent. The capacity audit used original native/29-joint goals for the frozen BFM prior, so its large corrections also reflect that declared goal/prior mismatch.

**One recommended next experiment, after recursive feasibility is established:** a single walk003 pilot of a **range-complete BFM-conditioned student with one teacher-gated dataset-aggregation round**. Keep the existing 1069 features, 256-by-256 network, frozen BFM weights and exact current observation/action-history arithmetic; use an unconstrained linear additive target correction followed by the unchanged native target bounds. A zero output must reproduce the paired BFM implementation exactly. Preserve all source timestamps; retain the already qualified BFM yaw-4 terminal controller instead of learning the old noisy MPC hold. No clip identity, time index, stored MPC state or saved gain may enter the network.

Use the qualified actual walk003 entry/source/return as the initial demonstration. Test the resulting student in one uninterrupted native lifecycle; query fresh expert corrections only at states/history actually reached by that student. Admit a queried correction only with independent physical and intent checks plus a successful continued return/hold; a feasible 600 ms local plan alone is insufficient. Preserve rejected queries and stop if the expert cannot supply that continuation. Fit one aggregated dataset and rerun the same nominal lifecycle once; do not run a radius/epoch/motion sweep. This directly tests the missing state distribution while removing the demonstrated representation ceiling. It does not assume either issue is sufficient on its own. The prerequisite matters: the current PICO hard-feasibility work has not yet established recursive viability, so starting a broad distillation job now would promote unqualified labels.

Acceptance remains the existing 15 full-source/lifecycle gates plus separate unchanged quiet-standing diagnostics, with every 2 ms native range/speed/effort/clock/warning evidence. Report teacher-state error and actual student-state error separately, and measure policy execution time separately from offline teacher planning. A nominal pass would justify a subsequent bounded robustness test, not live teleoperation. Current feature/reference support still includes simulation root information and a 740 ms packet buffer (up to 760 ms raw-pose derivative support); do not represent this as hardware-observable, low-latency Pico control.

Exact evidence roots:

- Student fits/labels/capacity: `E:/codex_sonic_runtime/mpc_student_20260910`.
- Latest PPO evaluation: `Z:/codex/GR00T-WholeBodyControl-sonic-transfer-23dof/artifacts/teleop_six_hour_20260910/residual_milestones_v1/residual_00800/summary.json`. Some old summary foot metrics use each pose's own heading; this review does not substitute them for the stricter same-world relative-foot gate.
- Qualified nominal hybrid: `E:/codex-artifacts/sonic23_teleop_six_hour_20260910/bfm_online_intent_v2/walk003_terminal_bfm_yaw4_hybrid_v1`.
- Read-only inventory and array crosschecks: this directory. Existing source files/artifacts are unchanged.
