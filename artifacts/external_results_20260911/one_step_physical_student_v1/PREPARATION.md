Trainer preparation only. No fit is selected or launched.

`source_draft_v1/fit_physical_continuation.py` derives from the reviewed final velocity trainer using the exact substitutions in `source_derivation.json`. All 32 original helper files remain byte-identical. Three separate physical modules implement requested-cell weighting, input/status binding and fixed endpoint diagnostics. No shared solver, reference, feature builder, normalization or controller runtime was edited.

The draft restores ordinary70000 model/AdamW/RNG/norm, then proposes exactly5000 updates70001..75000. Objective is original full3057 nominal + original sampled1152 velocity rows + all V qualified physical endpoints paired to live nominal successors. Each of the nine physical cells uses its requested denominator99/819/100; failures are retained and never boost surviving weights. The requested physical identities remain all3054, with separate final status and validity masks.

Maximum training rows36,315,000. Initial/final numerical diagnostics preserve the original1126 head calls and add2*ceil(V/256), maximum1150. Separate counters retain the original legacy hard limit. Torch rows max293,480; original14 analytical evaluations remain separately counted. No BFM or native calls occur in training/diagnostics. Windows batch parity is numerical; saved WSL branch-head outputs are compared without claiming cross-platform byte parity.

30 preparation tests passed using synthetic tensors, shape-only stub functions/sessions and source inspection. No real head was loaded or evaluated; no optimizer instance/update, actual ONNX session/inference or native simulation ran. Tests cover live successor gradients, base/target algebra, fixed weights with empty cells, phase transitions, exact call ceilings, strict status handling, manifest paths, review-subject binding, partial inference exception/nonfinite/extra-output preservation, unchanged helpers and exact trainer derivation. The earlier29-test XML is preserved; final report is `preparation_tests_final.xml`.

No training request, frozen-input receipt, clearance, launcher or fit output exists. The source deliberately requires those real documents before model construction. Final collection outputs, immutable validity mask, completed producer/call/native receipts, independent data and exact-conflict audits, source review and root fit selection are still needed. A concrete request must bind the actual V and all per-array hashes; placeholder subjects cannot satisfy launch gates. This draft does not qualify the controller or authorize a canonical simulation.
