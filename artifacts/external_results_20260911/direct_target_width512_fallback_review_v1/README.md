Saved evidence supports a coverage experiment before another architecture change. Older failed residual-student state was freshly replanned and qualified. Recent direct/context departures have only fixed-map diagnostics and local nominal-neighborhood labels. These are different forms of supervision; wider fitting does not create missing fresh expert decisions.

The prior `student_3s_fit_v1/student_snapshot.py` is a 1023→256 ELU→256 ELU→23 MLP, despite its “linear” output name. It does not test a global affine state-feedback controller. Teacher-state target RMSE was 0.027263 rad, but nominal execution failed before source motion. See [earlier evidence review](../fast_controller_evidence_review_v1/README.md) and the exact primary source/report subjects in `report.json`.

Actual saved-gain controllers were tested. Full walk002 fixed-plan feedback replay admitted 0/17 cases. Noise-scaled SVD gain cap 0.04, with original ±0.1 correction clipping, admitted 0/1 nominal and 0/2 perturbed cases; adding ankle-roll repulsion admitted 0/3. Nominal no-barrier execution completed 1,417 controls but exceeded a native joint bound by 0.001824873 rad. Feedback p95 around 0.08–0.092 ms measured only the local correction section. These historical evaluators used an online bound-abort allowance of 0.01 rad and final stricter eligibility; their abort times must not be compared as identical to today's every-substep strict oracle. They also indexed `planned_state`, target and gains by saved control number. This rules out those concrete fixed-sequence/capped/barrier variants, not every possible local controller or a goal-conditioned selector.

The current committed-map formula is `native_clip(u_plan + clip(K @ difference(x_plan, x), -0.1, 0.1))`. All 3,057 original/query1/query250 nominal commands reconstruct exactly. That establishes saved execution algebra. It does not make K a derivative of a fresh MPC solve or prove off-trajectory stability. K comes from the last accepted rollout's backward sweep; contacts, incumbent plan, seed choice and solver acceptance matter. Saved gains include extreme joint-position sensitivity, tiny unclipped neighborhoods, and 50 zero-gain controls across query1/query250 caused by no accepted update. Zero committed gain is not evidence that fresh optimal control has zero sensitivity. See [three-cohort audit](../three_expert_feedback_audit_v1/README.md) and [gain-supervision review](../feedback_gain_supervision_review_v1/README.md).

Coverage chronology:

- Query1 restored exact precontrol1 of a failed final20000 residual student: all 291 integration fields, warnings/clock, measured state, incoming prior and named history. A fresh expert branch then qualified the original full 1,569-control lifecycle and continuous 250-control hold. Its 1,268 moving labels retain exact query-state parity. This is genuine failed-student-state DAgger support; the interrupted collection resumed the same branch, not a second query. [Preparation](../fresh_student_expert_adapter_plan_v1/README.md), [completed label admission](../fresh_expert_labels_resume_v1/README.md).
- Query250 begins at actual original-BFM standing-prefix state. It is a genuine fresh actual-state expert branch and later direct-controller activation anchor, but is not a recent failed learned departure. [Branch source contract](../bfm_entry250_actual_oracle_v1/README.md).
- The 3,054 physical examples are independent one-control branches from those three nominal cohorts, using the frozen 70000 residual policy. Endpoints are real native states, but labels are the same-clock saved committed map with both clips; the request explicitly declares zero new MPC queries. They are neither connected learned-rollout recovery trajectories nor fresh replans. [Completed collection](../one_step_policy_branch_collection_resume2969_v1/RESULTS.md).
- Full58 adds signed one-axis local states around the same 3,057 centers, with center history/prior held fixed in the context study. Its 354,612 targets use the same committed maps. Static state admissibility and no feature aliases do not establish dynamical/contact feasibility or coverage of coupled departures. [Probe design](../direct_target_full_state_secants_v1/DESIGN.md).
- No fresh replanning of later direct/context 5000/55000/65000/68000/71000 failures was found in the inspected sources and outcome reviews. This is a bounded evidence inventory, not proof that no other file exists. Actual-state DAgger exists, but its qualified centers precede those departures.

A fast local-map library remains an untested hypothesis in this inspected evidence. Anchor states, goals, targets and gains could be fixed model parameters, but runtime selection would need legitimate measured state, actual incoming context and received goal preview. It cannot use clip ID, control index, hidden source frame, clock time as a reference lookup, or alter the original goal timeline. Goal-relative root/heading transforms, quaternion charts and velocity frames must be explicit. Contact/plan ambiguity, switching or blending, and large gains remain unresolved. The 23×58 gain product is small; selector cost and full-loop behavior have not been qualified. Old saved-sequence timing does not qualify a new selector.

Most informative next option: one prespecified early off-trajectory state from the failed width81000 trace, chosen after its saved semantics audit. First departure before clipping is a useful selection rule, not a selected control yet. Restore exact full integration/warnings/clock and causal incoming prior/history, supply original goals, perform one fresh expert query, and qualify the entire remaining original lifecycle plus hold. Compare fresh action/continuation with the existing same-clock map on that same state. Preserve failure; do not hunt through states until success. If qualified, retain that connected branch as new DAgger evidence before choosing any fit. This tests missing recovery coverage directly without assuming it is the only cause. Exact covered activation errors already show approximation error can coexist with the coverage gap.

Second option, after that evidence: source-only design for an explicit goal-conditioned local-map selector or local policy model, with proper charts and original runtime scope. No implementation or execution selected here. Repeating saved-index replay, gain caps/barriers, another blind duration increase, or claiming nominal K as fresh replanned truth is unsupported.

This review reads source, Markdown and saved JSON only. No task arrays, checkpoint deserialization, model inference, native steps, optimizer updates, replanning, or new controller execution.
