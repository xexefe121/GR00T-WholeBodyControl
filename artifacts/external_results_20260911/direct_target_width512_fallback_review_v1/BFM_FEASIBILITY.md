The inspected evidence makes zero learned-phase BFM calls a selected direct-head experiment contract. `direct_target_student_v1/DESIGN.md` records root's selection of a fresh absolute-target model; `direct_target_student_evaluation_v1/README.md` explicitly guards actor/backward calls during that model's learned phase. No inspected artifact establishes a global original-user prohibition. The original user wording is not reconstructed from these design files. Earlier selected residual experiments explicitly ran frozen BFM plus a head, so allowing that architecture as a new proposal would not itself contradict the historical experiment family. Every current frozen direct run still retains its zero-call contract.

BFM plus a small correction is computationally plausible on recorded proposal timing, and already has measured examples. The uncapped 75000 residual run logged p50/p95/max 6.820849/8.714435/14.947199 ms and zero 20 ms proposal misses over all 320 issued commands, including 70 learned BFM-plus-head commands. The component-selective applied-feedback variant logged 6.767499/8.853158/14.941698 ms and zero misses over 315 commands. Source `LinearStudentRuntime.propose` times measured terms/history, BFM goal/actor, feature construction, head, target and action arithmetic. It excludes the independent plant/IPC loop and native substeps. These results support proposal-budget feasibility, not a 500 Hz plant or complete asynchronous deadline qualification. The current width report's mixed-phase 6–14 ms values are likewise not a learned-only timing estimate.

Behavior rejected those residual candidates. Original 75000 stopped at control319/substep3 with left-ankle-roll bound excess 0.004744047 rad; variant stopped earlier at314/substep6 with left-knee bound excess 0.006958011 rad. Neither reached original source motion. Earlier 65000 phase residual stopped at278/substep4 on speed, and 70000 velocity-chord residual at261/substep6 on speed. All these recorded proposal timers reported zero 20 ms misses. Returning to frozen BFM plus the same trained correction therefore has no existing behavioral qualification.

The earliest truly capped residual, `bfm_residual_fit_3s_r025_v1`, also had a proven representation problem: 0.25*tanh could not represent 8.1826% of its short-data components; full walk003 required larger residuals in 40.2612% of source components, with best capped applied-target source RMSE 0.248806 rad. Later range-complete linear residual output removed that ceiling, yet still failed. “Small head” should mean computational size, not silently restoring a small correction radius. The 0.25 cap cannot be justified by the current teacher data.

BFM's goal/prior convention also matters. Prior capacity evidence used original native/29-joint BFM goals against a native-v4 MPC teacher; required correction includes that declared mismatch. A BFM residual has a state-dependent base and base features, so explicit response training must include both BFM response and residual response. Direct absolute-target training removed that base dependency; its weaker behavior does not isolate whether BFM is helpful. There is no matched current-width/current-context BFM-versus-direct recovery experiment here.

Reasonable interpretation: zero BFM is an architecture-specific active contract, not an established universal performance requirement. A separately reviewed range-complete BFM-conditioned controller remains a feasible candidate, but fresh actual-state expert coverage is still missing for recent failures, and timing headroom alone supplies no stability evidence. No controller change, extra inference, benchmark, fit or replan is selected by this note.
