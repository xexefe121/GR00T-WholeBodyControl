Recommend one conditional candidate: qualified original BFM for the declared initial-standing phase, then the unchanged residual network for acquisition, source and return. Fit once with deterministic full-batch gradients and a fixed declining learning rate. This combines a lifecycle split with a precision-focused fit; it does not isolate every change causally. No fit is authorized by this review.

First require independent full qualification of the BFM250 expert branch: all 1,569 lifecycle controls, 819 source controls, return and quiet tail, plus separate 250-control quiet hold. Only then collect actual expert controls 250..1268. Rebuild all 250 BFM history updates without labels or actor inference; retain raw BFM249 previous action. The first label uses qpos[250] and integration_before[250]; source frames are 261..1279; endpoint states qpos[250:1270] have length 1,020. Where the saved fresh BFM seed exists, compare the first clipped collector base with its first target. No extra expert query or student-control1 fixture is needed.

Keep three qualified datasets: original nominal, query1 actual-state, and new BFM250 actual-state. Filter each to 250..1268, giving 3,057 rows: 100 acquisition, 819 source, 100 return per dataset. Exclude initial-standing and terminal labels. The residual is disabled there. Audit exact duplicate target/residual conflicts and fixed cross-dataset nearest pairs before fitting; stop on exact conflicts, without averaging, smoothing or deleting commands.

Keep 1069 inputs, 256/256 ELU layers, linear 23-value residual scaled by native joint span, original mean/std and span, and all BFM/history arithmetic. Add no time, clip ID, planner state or history input. Supervisor mode reads the already declared lifecycle phases outside the neural network; it cannot change source timing or become a learned timestamp shortcut.

Restore ordinary final60000 model, all six AdamW states and steps, and all RNG states after constructing objects. Check original saved predictions and restored ONNX bytes exactly before changing optimizer settings. Use a fixed row order: original/query1/query250, ascending control. Each update forwards all 3,057 rows once. Loss is the average of nine dataset-by-phase cell means of 23-dimensional normalized residual squared error. Each cell therefore has equal weight without random sampling. Keep CPU/BLAS threads1, gradient clip10, weight decay1e-5.

Exactly 5,000 additional AdamW updates, global60001..65000. Local k=0..4999 uses lr = 3e-6 + 0.5*(3e-5-3e-6)*(1+cos(pi*k/4999)). No moment reset, warm restart, checkpoint selection, EMA, early stop or extension. This is 15.285 million row presentations, compared with 10.24 million for the prior40,000-by-256 fit: a bounded workload, not evidence that convergence is assured.

Evidence for this choice: failed aggregate control0 exactly matches a qualified teacher input, including all300 history values, but has0.0218575rad known-target RMS error. Prior action leaves training ranges by control4. Late minibatch diagnostics fluctuate: old/new RMSE0.02515/0.02531 at55000 versus0.03156/0.03189 at ordinary60000. Full-batch removes sampling variance; lower rate targets fit precision. AdamW and nonlinear models can still behave nonmonotonically. Standing mode bypasses the old control0 residual problem, so first active control250 is now the critical equivalent measurement. These observations do not prove missing-history dependence or justify direct K-gradient supervision.

At initial model and every250 updates, record the fixed full objective, all nine cell target/residual RMSE, per-joint p95/max, first24 controls of every phase, all23 signed errors for new query250, and fixed nearest-pair predictions. Diagnostics cannot select the model. Save ordinary final65000 model/optimizer/RNG and full prediction arrays before export. Stop without rollout if the final full objective is nonfinite or fails to improve over restored60000 on exactly the same rows. Keep per-cell regressions visible. There is no evidence for an absolute imitation-error threshold that certifies control safety. Require Torch/ONNX residual parity below1e-5 on all3,057 rows and a frozen source/export review.

One canonical rollout then starts from fresh canonical initialization. It generates all250 original BFM standing controls with residual disabled; no saved state, target or history injection. Before activation, require exact matching of all2,501 states,2,500 torques and available target/action/history fields to the qualified baseline. At actual precontrol250, features, base target, previous action and history must match the first new label before applying the learned command. Keep the actual plant and history continuous through the switch. Residual covers all100 acquisition,819 source and100 return controls; existing terminal yaw4 BFM begins1269, followed by the separate continuous250 hold. No arbitrary fade, cap, activation delay, reset or acquisition/source skip.

Acceptance: one uninterrupted1,569-control lifecycle, all original every2ms native and source-intent gates, both unchanged quiet diagnostics including separate hold, independently reproduced. Report inference p50/p95/max and20ms deadline misses separately. Any parity or physical failure ends the run with raw partial evidence. Source/quiet failure remains failure; no immediate refit, switch adjustment, retry or new query. A nominal pass supports a later bounded robustness assessment, not hardware or live Pico readiness.

proposal.json fixes the exact unexecuted candidate and stop criteria; input_hashes.json binds current evidence. New branch qualification remains pending. Reviewer inference, optimization and physics counts are zero. No existing source was changed.
