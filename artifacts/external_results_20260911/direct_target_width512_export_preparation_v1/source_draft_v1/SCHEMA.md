This is an unselected future export schema and source preparation. No actual ordinary-81000 checkpoint or deployable release exists in this package. Synthetic graph files are test fixtures only.

The future source is the single fixed ordinary-81000 endpoint of the proposed 10000-update width continuation from ordinary71000, with optimizer step16000, causal input1323 and architecture1323→512→512→23. Existing normalization and all six final actor tensors must remain original float32. The exporter requires kind `direct_absolute_native23_target`, ordinary_final_step81000, exactly the six actor keys0/2/4 weight/bias and exact layer dimensions. Positive finite float32 normalization is required. Future input freezing/release checks must additionally bind causal condition, context order, source71000 lineage, optimizer/RNG restoration, completed fit/data evidence and actual final checkpoint hash; the small promotion module is not a complete release gate.

Public ONNX input `features` is float32[batch,1323]; public output `normalized_target` is float32[batch,23]. All normalization, three dense contractions and two ELUs execute in float64 using exact promotions of the final float32 parameters. Only the public input and final output have casts. ELU(alpha1) is represented as `Where(x>0, x, Exp(Min(x,0))-1)` using double constants0/1. The graph has20 nodes,10 embedded double initializers, opset17 and IR8. No clipping, task feature assembly or native default/span arithmetic is embedded in the graph.

This retains the original mathematical network but uses monolithic double contractions rather than the training model's split float32 contractions/custom zero-preserving addition. No byte-exact cross-backend output claim follows from parameter roundtrip or graph validation. Final numerical admission retains the original CPU64/GPU64/ORT64 pairwise reconstructed-preclamp-target gate of1e-5rad. Final GPU32-to-FP64 drift remains a separate diagnostic without this admission gate. Initialization retains the original expanded-GPU32 versus saved71000-final-GPU32 comparison gate of1e-5rad plus byte evidence. No new GPU32-to-FP64 admission rule is introduced. Exact target reconstruction remains default_f64 + existing_span_f32.astype(f64) * output_f32.astype(f64), followed by unchanged exact native bounds clamp at runtime. Span must not be recomputed from float64 bounds.

Future report requirements: actual checkpoint/head/request/frozen-input hashes; ordinary_final_step81000; features1323; head_output normalized_target; internal_dtype float64; public input/output float32; fixed selected diagnostics row/call counts; finite max_preclip_error_rad≤1e-5 and explicit numerical/export-parity pass flags; known raw exit, unchanged source/input pins and observed process absence. These are requirements, not populated results. No failed original release is overwritten or bypassed.

Only parameter promotion/copy ownership, static graph structure/type/shape, and source derivation are tested here. No Torch forward, ORT session/run, CUDA initialization, actual checkpoint/task-array read, training or native step is performed. Actual numerical parity, batch-one latency, physical stability and original1569+conditional250 acceptance remain unqualified.
