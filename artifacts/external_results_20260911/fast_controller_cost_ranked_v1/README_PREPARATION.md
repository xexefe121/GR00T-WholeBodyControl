# Single cost-ranked candidate experiment

Parent selected one canonical1569-control lifecycle, followed by one continuous250-control terminal hold if the lifecycle reaches its end. Final independent source/launcher CLEAR is required before launch. This artifact contains the prepared controller; the preceding first-feasible trial and132 fixed counterfactual forecasts remain unchanged.

Initial controls0–249 use the original disabled-head BFM exactly. Full prefix, actual query250 input and frozen65000 primary proposal must match their existing fixtures before the first learned command is applied. Moving and terminal admission evaluates all four targets in their original order: primary, original BFM, previous actually applied target, current measured joint pose. Even exact duplicate candidates are forecast and scored independently. Every target receives the same fixed100ms early-stop native forecast; every2ms strict gate remains unchanged.

Only candidates completing all50 private physics steps with a feasible verdict are eligible. Selection uses the smallest exact qualified query250 tracking-cost prefix: original state residual squared at knots0–5 plus original target penalties at0–4. Initial state cost is common. Exact score ties retain the first candidate in original order. The H30 constructor, all cost weights, interior margins, reference frame indexing and model geometry are unchanged; no horizon normalization or terminal multiplier is added. No optimizer or planner dynamics is called at runtime. The score is a truncated prefix, not the complete H30 objective.

Only20ms of the selected target is applied before fresh admission. Moving history stores normalized actually applied targets; initial raw BFM and unchanged-primary terminal raw BFM history semantics stay intact. Proposals and applied commands remain separate. All-rejected admission preserves actual full integration, named/flat history, raw prior action, count and warnings without applying a rejected command. Actual applied control arrays remain aligned. Every selected first10 forecast samples must match actual qpos/qvel/torque/force/time/warnings exactly.

All candidate physics and available cost features/residuals/components/references are saved. Infeasible partial forecasts are censored and never ranked by their shorter finite cost. Empty, partial and unscored-fault cost records preserve explicit dimensions and availability masks. Timing includes every proposal, all four forecasts and copies, cost evaluation, selection, actual physics and recording. Separate ranked-attempt summaries include rejected attempts. Latency will likely exceed20ms; this remains a synchronous simulation experiment, not a received-stream or hardware readiness claim. Short-horizon cost may favor standing, so full source tracking and both quiet holds still require independent qualification.

Saved132 equivalence checks passed for every available feature, residual, component, state/input cost and reference array; all33 diagnostic rankings match. Duplicate/tie/infeasible/nonfinite tests pass. No new forecast, actual-plant step, inference, fitting or label collection ran in these prelaunch tests; only forward kinematics and saved-array arithmetic were used. The first test exposed an older pilot copy of the tracker API before any case evaluation. That constructor-only fault is preserved, and the three cost dependencies now match the exact qualified query250 versions. Explicit package files ensure frozen module resolution.
