Preparation only. No controller inference, private forecasts, physical rollout, or fitting has run in this artifact.

The proposed controller uses the unchanged ordinary final65000 weights and original BFM references. Initial BFM controls0–249 remain exact. At250, actual full state, measured history, prior action, BFM base, and1069 features must match the qualified query250 input before admission. During the moving and terminal phases, candidate order is primary / originalBFM / preceding actual applied target / current joint pose. Each bounded target is considered over100ms in private native physics; only20ms is applied, then admission repeats from the fresh actual state. All-failed admission terminates without changing actual plant, measured history, prior action, or control count.

Moving-phase action history is normalized from the selected actually applied native target. Standing retains original rawBFM semantics. Terminal BFM retains raw action when its own target is applied unchanged; otherwise it records the selected applied target. Proposed target/action and applied target/action are recorded separately. This is a new controller experiment, not an isolated history-only ablation.

`source_draft_v1/transactional_student.py` prepares exactly one actor/head proposal using a copied history buffer. Actual history, prior action, and count change only on a valid commit. `ordered_admission.py` implements the fixed first-feasible order and exact numerical duplicate accounting, with forecasting provided by a callback. Stub tests cover cancellation, duplicate/stale commits, invalid targets, mode flags, nonfinite proposals, action conventions, all-rejected admission, and duplicate candidates. These tests made zero real inference calls or physics steps.

Integration of the reusable private forecaster remains blocked until its separately authorized232-case parity benchmark passes. Runtime source freeze, root inspection, and independent review are required before any canonical rollout. Shared repository files and previous source snapshots remain unchanged.
