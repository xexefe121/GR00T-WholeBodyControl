This packet prepares one future recorded-expert command benchmark. It does not select or dispatch the benchmark.

The reviewed pending-BUSY core retains one immutable job and serialized payload after an unambiguous BUSY result. It permits one publication attempt per eligible physics tick, at most ten attempts total, while preserving the original activation and admission deadline. Other errors and ambiguous returns are terminal. Worker-result BUSY handling is unchanged.

All original external runtime, native, reference and fixture pins are retained from the 3,718-pin first benchmark launch. The current 22-file producer and 17-file retry-aware saved auditor have independent root source reviews. The old one-publication auditor cannot audit this ledger.

The scope remains 1,569 main controls plus a continuous 250-control hold, 18,190 native steps, four model serializations, no model or optimizer calls. Native 2 ms and command 20 ms deadlines, 60 s elapsed abort, debt limit 100 and one future epoch remain fixed. Watchdog budgets remain setup 240 s, plant 120 s, preservation 180 s and outer timeout 555 s plus 5 s termination grace. These watchdog budgets do not relax individual deadlines.

The previous completed benchmark failed timing before its BUSY publication and later failed native joint bounds. This source change does not establish a timing remedy, online policy performance, balance or real-time qualification. Root must review the concrete packet and separately select execution during an idle compute slot. No clearance, process, run output or stage receipt is created by this preparation.
