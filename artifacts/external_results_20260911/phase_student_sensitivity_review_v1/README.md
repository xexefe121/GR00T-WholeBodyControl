The first learned input is exactly correct. At control250 the complete saved feature vector, BFM base, state, prior action, and history match the qualified expert label; the ordinary-final head still has target RMSE0.0319578rad (worst joint0.08904rad). This is fit error at an observed training input, not evidence of a feature-order, checkpoint, or simulator-state mismatch.

One control later, head output has moved0.149672rad RMS from its prediction at the corresponding expert-state input, while saved BFM base moved only0.006698rad RMS. A straight-line integrated-Jacobian decomposition attributes0.160842rad RMS to the measured joint-velocity block,0.033765 to root angular velocity,0.017075 to previous-target features, and0.015309 to explicit raw prior action. These correlated contributions partly cancel; their RMS values must not be added. They describe the fixed model's input/output change, not independent causal interventions on physically realizable states. Subsequent physical divergence changes BFM state/history and goals expressed in actual heading as well.

Both action-feedback paths matter. With BFM output, goals, and physical state held fixed, the conditional target-equivalent previous-action Jacobian has spectral norm7.36 and spectral radius1.54 at250; at251 these are7.77 and1.39. Those are conditional neural recurrence values, not eigenvalues of the robot's complete closed loop. The head's joint-velocity derivative norm is1.70s at250 and2.54s at251. Full300-value BFM history is not a direct head input; its effect cannot be separated from actual-state changes using saved base outputs alone.

The first target clipping occurs252. Student preclip combined-action history then differs from expert history based on the actually applied target; that mismatch reaches direct prior input253 and history-action channels254. It is a real later semantic mismatch, but growth at251 precedes it. A history-only ablation cannot explain the initial amplification. Exactduplicate conflicts were absent, and lower fitting loss does not constrain off-trajectory derivatives. Evidence supports observed pointwise underfit plus sensitive learned feedback; it does not prove insufficient network capacity, missing history, or a unique causal fix. Actual-versus-sameclock expert target differences after divergence are not fresh expert labels at the student's state.

The native forecast evidence makes a filter a more concrete next control experiment than another unconstrained fit. All29 recorded student20ms forecasts reproduced actual samples exactly. A100ms constant-target forecast rejects the student already at261, before the observed20ms violation278. At278 BFM passes20ms but fails100ms, while previous applied target and current-q hold pass100ms. This illustrates why a single20ms pass is weak and why no one fallback should be assumed safe. It does not show that a filtered closed loop will preserve tracking or remain recursively feasible.

Recommended next bounded work: validate a preallocated full-integration native forecast component against all232 fixed oracle cases, including state, force, warning, clock, failure location, and caller immutability, and measure complete cost. Current deepcopy forecasts cost roughly53-73ms at20ms and62-83ms at100ms (median-p95 under contention), so they do not establish50Hz readiness. This component work is authorized separately; no connected fallback policy is selected here.

If that component passes, the next candidate should be one frozen-model simulation wrapper with a fixed100ms candidate forecast and predetermined order: student target, original BFM target, previous applied target, current-q hold. Every candidate must satisfy unchanged strict2ms native constraints; no candidate means terminate the diagnostic without advancing or resetting the actual plant. Action-history bookkeeping must explicitly describe the actual selected controller command; align the residual mode to the expert applied-target convention or declare a separately controlled semantics ablation. Do not silently combine that correction with a filter and attribute the outcome to only one change. Preserve originalBFM250 entry, all1019 learned-phase source/acquisition/return controls, terminal mode and continuous hold; require the existing independent full-source/quiet gates. This is a conditional proposal, not authorization or a safety guarantee.

Blind K supervision is weaker evidence. Saved MPC gains describe a local frozen-plan feedback law, not necessarily the derivative of a newly optimized policy; clipping, warm starts, contact switches, and the previously measured very large gains complicate it. A correct residual derivative target would also require the feature/tangent/normalization/span chain and BFM derivative, including history. Unqualified perturbed labels or arbitrary gain clipping would conceal this problem. Do not launch another fit, new expert query, or K-regularization sweep from the present diagnosis.

Numerical checks: float64 evaluation of stored float32 head weights matched recorded ONNX outputs within3.88e-6rad; analytical derivatives matched central directional differences below1e-7. Adaptive64-2048-point integration closes every seven-frame output difference within1e-5rad. Initial64-point attempt failed its quadrature-accuracy assertion and remains preserved. No BFM inference, dynamics, optimizer update, or new candidate was executed in this sensitivity study.
