# Width81000 saved-fit auditor: source preparation only

Prepared from the completed, corrected `direct_target_response_balanced_fit_independent_v3`. No actual audit request, launcher receipt, output directory or audit process exists. Actual execution requires the completed ordinary81000 fit and owner, then root source and concrete reviews.

## Review map

| File | Change from qualified71000 auditor |
|---|---|
| `audit_saved_warm.py` | Width-specific request/source/checkpoint/report metadata; exact71000 source; full10000 schedule and preserved3000 prefix; expanded initialization; warm6000→16000; new LR ramp; width512 graph/promotion shapes; training30000 calls/146860000 rows. Same failure-aware evidence/numerical distinction. |
| `audit_width_math.py` | New independent CPU tensor reconstruction. Explicit concatenation of old256 blocks, zero outgoing blocks and old/new moments; four local-generator draws in declared order; exact seed/state, source/global RNG, whole AdamW group and normalization. No producer import, model, gradient or optimizer construction. |
| `audit_graph.py` | Only three literal weight shapes change from256 to512. Same20-node graph, normalization, exact FP64 promotions, bounded ELU, public float32 contract. |
| `test_width_audit.py` | New23 synthetic checks: saved-state and source corruption, exact local RNG, old signed zero, shared optimizer age, ramp boundaries, unchanged reduction/parity gates, old-width graph rejection. |
| `test_producer_metadata.py` | Four isolated AST checks against frozen producer protocol/restoration/report/checkpoint/drift schema. No fitter import. |
| `test_prior_math.py` | Synthetic graph fixture shapes only become512; prior context/history, drift, scheduler-prefix and graph corruption tests retained. |
| `test_metadata.py` | Request-writer experiment path only. Prior duplicate-key and top-level concrete review regressions retained. |
| `prepare_audit_request.py` | New experiment/kind and ordinary81000/optimizer16000/hidden512 admission. Requires completed optimization, diagnostics and positive owner accounting. Numerical failure remains auditable. |
| `freeze_launch.py`, `run_audit_durable.ps1` | Saved-audit kind only. Same immutable output/lock, hidden captured process, known raw exit, pre/post hashes and no retry. |
| `verify_completion.py` | Unchanged. Same saved owner accounting, PID absence and evidence/export distinction. |

Nine source modules remain byte-identical: the balanced54, full58, context, original nominal/physical metric arithmetic; exact tensor/tree comparator;16 release paths; and the prior rule/reduction/balance regressions. `source_delta.patch` gives all inherited-file changes. New files are listed directly in the preparation receipt.

## Required contracts preserved

- Original nominal CUDA-vs-f64 mean check uses relative3e-7. The invalid redundant NumPy-f32 reduction-order check stays removed. Both original and balanced54 cell ledgers and totals remain checked.
- Five367570-row saved output corpora retain1437 partitions each. InitialGPU32 uses the original1e-5rad gate, with byte equality reported only. Nine finalCPU64/GPU64/ORT64 comparisons keep1e-5rad. FinalGPU32-to-FP64 drift stays diagnostic.
- Independent full10000×864 PCG64 reconstruction checks all8.64M pair slots, original3000 prefix and actual saved source/copied schedules. No schedule generation occurs in the producer; generation here is saved-evidence reconstruction.
- Exact shared incoming context, physical applied prior/once-advanced history, normalization and16 literal release subjects remain checked. Diagnostic batch size remains256;512 means hidden width only.
- Added optimizer entries start with zero moments but retain shared step6000. Old actor/moment blocks and all learned context columns remain exact at initialization. Final six scalar steps must be16000; RNG must remain source RNG.
- Saved evidence cannot establish authenticity of outputs/gradients without recomputation. The auditor makes no model/ORT/native calls and does not claim closed-loop success.

## Validation and preserved preparation failure

73 inherited/new synthetic tests passed, then four additional frozen-producer AST metadata checks passed. An initial metadata-test collection-size guard incorrectly expected more than30 report fields; the actual isolated loops contain27. That test-only guard was corrected to exact27/13 report/checkpoint counts; the failed XML is preserved. No auditor production source or required gate changed for that test correction. Root can run all77 tests together.

Actual fit artifacts remain untouched. No actual audit has been attempted.
