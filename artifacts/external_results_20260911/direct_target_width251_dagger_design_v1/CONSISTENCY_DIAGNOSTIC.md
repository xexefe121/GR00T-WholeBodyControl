# Minimum conditional consistency check before choosing a fit

No arrays or model calls are selected or performed by this note. First require qualified collection. The diagnostic is intentionally split into a pure saved-data part and an optional fixed inference part because new-state prediction errors cannot be recovered from old model outputs at different states.

The deterministic student receives1323 raw features, but the expert also carries warm-start plans, committed-plan position and solver selection history. Those internal planning variables are not necessarily functions of four sensor-history lags and the eight goal-preview slots. Consequently, identical or nearby student inputs can have different expert targets. Full physical/intent qualification makes a label valid for that executed expert continuation; it does not prove that all labels define a single-valued target function under the student's inputs.

## Saved-data part: zero models, gradients or native steps

Use all1018 new rows, the existing9904 nominal inputs/absolute targets and3054 physical endpoint inputs/absolute targets. Keep these13976 records separate and retain original dataset/phase/control/endpoint provenance. Physical endpoints must use their actual applied-prior and once-advanced history, not the nominal center context or previous raw prior. Compare against `physical_target`, not the physical finite-difference label. No full58 probe expansion or new labels are needed for this minimum check.

Hash each finite float32[1323] input row and verify full row bytes before calling a hash match an alias. Group every exact alias across old/new and within new; report group members, actual float64 target-radian ranges, per-joint differences, RMSE/max difference and differences after the unchanged float32 normalized-label cast. Exact raw-input duplicates with materially different targets cannot both be matched exactly by a deterministic head. Report every disagreement, including tiny numerical differences, then distinguish cast-equivalent labels from materially inconsistent labels. Do not average, drop, reweight or choose one label automatically.

Also report how often current1000 features alias while prior/history distinguish the rows. This checks whether the causal extension resolves collisions that would exist under current-state-only inputs. A secondary NumPy float32 frozen-normalization alias check may expose rounding candidates, but must be labeled as that calculation; it must not be promoted to a proven CUDA kernel input identity without a matching arithmetic proof. Raw1323 identity remains the decisive exact feature test.

For a bounded proximity view, select the first24 chronological new rows from each phase,72 rows total; state251 is included. Compare each with all12958 old nominal/physical endpoints. Use fixed1323 feature means/stds, promoted to float64 for distance reporting, and blocked distance reductions with a fixed tie rule based on original row order. Report nearest matches for full1323 and current1000, plus the three block distances(current1000, prior23, history300), absolute target jump, per-joint maximum, dataset/phase and clipping flags. Keep nearest-neighbor calculations diagnostic: they do not choose a controller center, replace goals, or introduce control index as a feature. Phase-matched nearest comparisons can be an additional view of the same72 queries. Absence of conflicts in this bounded proximity sample does not establish global smoothness or coverage.

No universal proximity threshold should manufacture a contradiction. Preserve the actual distances and target jumps. A large label jump at exactly equal inputs is decisive; a jump at merely nearby inputs may be legitimate sensitivity, finite-horizon plan ambiguity, or insufficient density. Saved plan-control/local-index and gains can help interpret it, but are not extra permitted head inputs.

## Optional fixed new-input prediction diagnostic

To know new-row errors before selecting training, root could separately select one qualified81000 FP64 ORT pass over exactly1018 collected inputs at batch256: four calls,1018 rows, no Torch forward, gradients, optimizer or native work. Save every returned float32 normalized target before conversion. Report all-row and each three-phase normalized MSE, raw/applied target RMSE, per-joint/max errors, clip counts, and first24 chronological rows per phase. This also supplies the frozen81000 baseline for a later fit. No row or checkpoint selection follows automatically.

If an extra pre-fit model pass is not selected, those error metrics must wait for the new corpus's counted initial diagnostic inside a later selected fit. Do not reuse the old student target at another physical state, or same-clock old nominal predictions, as the student's prediction on the new trajectory. Only state251 has the existing exact actual-state/student-output comparison supplied by the root first-target audit.

The source/label consistency result should decide whether a simple additional supervised term is coherent. Clear conflicting targets at the same1323 input require a design decision before fitting; blindly appending them can encode an impossible regression problem. Large new-state support gaps with coherent targets support testing added coverage, while small support shifts and persistent high new-state error would favor a fitting/representation question. Neither observation alone proves that one1018-row expert continuation resolves future on-policy failures.
