# Proposed conditional output

No output below exists yet. `source_prepared_v1` is the reviewed candidate; actual execution requires future completed-recovery qualification, exact request pins and root selection.

`expert_rows.npz` has1018 rows, exactly controls251..1268. Arrays: `control/source_frame/plan_control/plan_local` int64; `phase` int8 (0 acquisition99,1 source819,2 return100); `first_student_state_query` bool (only first row true); `features` float32[1018,1000], `context` float32[1018,323], `causal_features` float32[1018,1323]; `incoming_prior` float32[1018,23], `incoming_history` float32[1018,300], and `incoming_history_{actions,base_ang_vel,dof_pos,dof_vel,projected_gravity}` float32[1018,4,width].

Actual measured `qpos` float64[1018,30], `qvel` float64[1018,29], `integration` float64[1018,291] are precontrol values. `expert_target/planned_target/feedback_raw/feedback_correction` float64[1018,23], `planned_state` float64[1018,59], `gain` float64[1018,23,58]. `feedback_clipped/native_clipped` bool[1018,23] retain the two distinct clamps. Target is actual applied expert target in radians; there is no target normalization or label clipping beyond exact reconstruction of the executed native command.

`normalization.npz` is an exact byte copy of the current width81000 frozen normalization. It is preserved for lineage; no statistics are recomputed. `report.json` reports collection completion, exact request/source/input/plan/output hashes, phase and clipping counts,1 fresh student query plus1017 connected expert rows, and zero task-model/BFM/native/replan/optimizer calls. It explicitly leaves model fitting unauthorized. On a failure after creating the exclusive output directory, `failure.json` preserves the exception and `collection_completed=false`; existing outputs are never overwritten and no retry is automatic.

The future request contains `root_selected_collection=true`, `model_fitting_authorized=false`, output directory, complete source hash map,204 ordered plan subjects (control/path/hash), and the exact roles in `qualification_gate.QUALIFIED_ROLES` plus `qualification`. Roles include the actual recovery request/owner, main and hold traces/reports, four root audits, boundary snapshot/review/extraction receipt, plans.json, original goal archives/native contract, frozen normalization, qualified core source, and collector source review. The root qualification must be collection-only, specify start251/stop-exclusive1269/rows1018, and directly match all those subjects and the204 plan subjects. No arbitrary digest anywhere in a report is accepted as a trace binding.

The future actual request will use the successfully completed recovery namespace. Current candidate is `direct_target_width512_expert_recovery_v2`; prior namespaces and failed attempts stay preserved. Namespace alone cannot qualify data: all runtime and independent receipts must pass first. No current actual request, qualification, copied training dataset, or launch packet is supplied by this source preparation.
