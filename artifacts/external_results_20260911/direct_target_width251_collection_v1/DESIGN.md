# Conditional collection from the fixed precontrol251 recovery

Source preparation only. No actual collection, new inference, native step, replan, optimizer update, or label admission is selected by this package. The recovery remains one prespecified experiment at the actual width81000 state immediately before control251. A failed or partial recovery cannot enter this collector.

Admission requires the completed recovery owner, raw and wrapper exit0, exact postrun hashes and absent processes; completed main1569 and continuous hold250; and four independent root reports with positive physical, original source/return and both quiet-window decisions. Each report must bind the literal actual trace path and hash; intent reports must additionally bind their physics report. A separate root collection decision must bind all those artifacts, the fixed request and this reviewed collector. Physics reports' own `trace_sha256` denotes the independent replay output and must not be mistaken for their input trace identity.

Collection contains exactly controls251..1268 inclusive:1018 newly executed expert targets. Acquisition251..349 has99 rows, original source350..1168 has819, return1169..1268 has100. Entire preserved prefix0..250, including the learned action250, and terminal1269 onward are excluded. `controller_mode == 1` alone is not a selection rule because learned250 also has that value. All204 committed plans from251 every5 through1266 must have completed their declared commits; the last commit has3 controls.

Each row keeps the actual incoming qpos30/qvel29/full291, raw applied-target inverse23 and named four-lag history300, source frame control+11, actual applied native-bounded target23 float64, original plan control/local index, nominal state59, nominal target23 and gain23x58. The unchanged pure DirectFeatures builds1000 float32 values using only measured state and original received goals. The unchanged incoming_context appends prior23 plus history in alphabetical field order to obtain1323. No BFM actor or backward inference is needed. Frozen81000 normalization is copied byte-for-byte as provenance; this stage does not refit statistics, normalize training targets, choose a loss, or fit a model.

The adapter verifies full291/qpos/qvel identity, incoming prior aliases, outgoing-prior chronology, named/flat history identity, and exactly one measured history update per issued control. Applied-target inversion is checked for all collected expert controls. Main final full291, prior and history must equal hold initial values. Selected state251 must equal the previously independently checked boundary snapshot. These are saved-array checks; no native replay occurs here.

For every collected target, the exact qualified plan difference chart and full gain product reconstruct the native command with the original nested +/-0.1 feedback clamp followed by native joint clipping. No grouped product, rearranged reduction, state clamp, goal substitution or label repair is permitted. The raw map correction, both clip masks and resulting target are retained. The source for the pure quaternion/difference functions is pinned and parsed by AST; the native/planner module is never imported. An actual collection would use the existing WSL NumPy1.26.4 environment to preserve the command reduction order. Synthetic preparation can use the local CPU runtime.

Only state251 is a fresh query from a failed student rollout. Later labels are connected expert-induced states. Saved gains describe each committed local expert plan; they are not fresh MPC solves at every row or perturbation. This package creates no perturbation labels. Any later full-state probes or training mixture need their own explicit selection and must preserve existing nominal/response benchmarks.

The draft exposes a pure adapter and a gated saved-file runner. A future concrete request must enumerate every consumed trace, plan, report, goal archive, native contract, frozen normalization and pure source by exact path/hash. Output is created exclusively and retains failure evidence. There is no automatic retry. No actual request is generated before successful recovery qualification and root source review.
