# Ordinary65000 full-state controller simulation preparation

Source only. No new head, witness, native rollout or release binding exists yet.

Preserve the preceding FP64 evaluator's native physics, full291 canonical initialization,1000 pure features, original span/default reconstruction, history/prior semantics, all1569 controls and conditional continuous250-control hold. Preserve one WSL batch1 head witness before canonical execution. Original limits, clocks, source coverage, startup/terminal BFM behavior and independent physical/intent audits remain mandatory.

Only release validation changes: ordinary65000 comes from the newly qualified full58-data fit, fresh AdamW10000 updates, fixed initial calibration, and a unified same-weight FP64 export. The older55000 FP32 failure/repair receipt structure does not describe this new fit. `evaluation_gate.py` selects65000 and the new explicit subjects; `export_release_gate.py` checks actual fit completion, full58 independent data audit and corrected owner v2, actual independent fit audit, final owner, source review and final release review.

The fit audit must distinguish evidence integrity from numerical qualification. Canonical execution requires both true; failed parity never becomes an inference-ready model. Every logical subject is matched by name or canonical path and literal SHA256. WSL/Windows paths normalize only drive/separators/case; unrelated paths cannot satisfy membership.

The final owner and release review name13 direct subjects: fit_report, checkpoint, head, normalization, training_manifest (input freeze), training_request, export_manifest (fit output manifest), coefficient, source_checkpoint (ordinary55000), full_state_generation_request, full_state_generation_report, full_state_data_audit, full_state_data_owner. The final release additionally binds independent fit audit and fit owner. Source review directly names module hashes.

All29 other copied Python modules remain byte-exact. Existing runtime/native stub checks plus new complete/rejected receipt-chain tests pass55 cases. No task weights, model sessions, native objects, online worker or robot are created by these tests. Source review, concrete future runtime bindings and actual qualified65000 output remain pending.
