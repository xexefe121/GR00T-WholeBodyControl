The strongest next change is **full-state, physically scaled finite-secant feedback supervision**, retaining the direct architecture and all nominal anchors. More unchanged-objective training or nominal phase rebalancing is poorly supported. The full-state teacher design still needs explicit perturbation scales and feasibility rules; no new fit or controller is selected by this analysis.

The numerically qualified FP64 policy failed at control 291/substep 6, 5.832 s, with native speed ratio 1.11995 and zero source controls. Its error at the exact shared control 250 state improved from 0.098651 to 0.045540 rad RMS. Yet control 251 error rises to 0.093250 with 0.417523 rad/s joint-velocity departure; at 252 these reach 0.305625 and 1.010634. First clipping is 256; 34 of 42 learned commands clip. Growth therefore precedes clipping. After 250, nominal target comparisons involve different states. See the [independent saved semantics](../direct_target_fp64_saved_semantics_review_v1/report.json).

The decisive coverage gap is larger than joint velocity. Existing axis probes cover only 23 of the expert's 58 tangent directions. The other 35 are root position/rotation, joint position and root linear/angular velocity. Their combined preclip feedback-change norm exceeds the joint-velocity group's in **37 of 41 departed rows**. Before the first learned clip, controls 251–255 aggregate to 0.2273 versus 0.1421 rad RMS; at 252 root angular velocity alone contributes 0.17623 versus joint velocity's 0.07949. These are correlated preclip linear terms, not additive causal percentages; both subsequent teacher clips matter. See [all 42 fixed-map comparisons](../direct_target_fp64_fixed_map_v1/report.json).

The observation already contains joint positions/velocities, root angular and heading-frame linear velocity, gravity, height, and eight future full-body goal samples. Removing BFM and direct prior inputs removed one feedback path but did not enforce stabilizing response. Native PD damping remains active; an MLP can oppose it through its position targets. An arbitrary diagonal damping/sign constraint would discard coupled balance behavior and is not justified by the saved gains.

Phase weighting already gives acquisition, source and return one third of nominal mass each. Each dataset has one fifth; three walk003 datasets therefore receive 60% of nominal mass and all off-nominal supervision. A query250 acquisition row has 8.19 times the weight of its source row. More acquisition weight could reduce the initial command error, but scarcity of acquisition examples is not the demonstrated explanation for growing departures. The 3,054 physical labels each add one coupled 20 ms displacement generated by an earlier policy; they do not span the missing directions or the current multi-step state distribution.

For normalized target `z=(target-default)/span` and joint speed `r=dq/native_cap`, the existing signed-endpoint error is

    e± = (f(x±0.01 axis)−f(x)) − (z_teacher(x±)−z_teacher(x)).

The current loss averages `e±²`. A finite secant per unit normalized speed divides by **±0.01**, so its squared loss is exactly **10,000 times** larger. A 2,500 factor would describe a different positive-minus-negative formula. This remains a finite secant through the exact two teacher clips, not a raw K derivative or replanned expert.

| Saved equal-cell metric | Direct 5,000 | Current 55,000 / FP64 |
|---|---:|---:|
| Nominal normalized MSE | 0.00146922 | 0.000448775 |
| Physical response MSE | 0.000193443 | 0.0000956462 |
| Velocity secant MSE | 0.555893 | 0.680371 |
| Zero-response error against those teacher secants | 0.334851 | 0.334851 |

Every velocity cell is worse than that zero-response baseline; 98.48% of current chord error is its odd/slope component. Even the 108,013 probes without either teacher clip have raw-row-weighted secant error 0.531463 versus a zero-response error of 0.135133. Clipping ambiguity alone does not explain poor feedback imitation. Conversely, a blind 10,000 multiplier would occupy 99.920% of the present scalar objective. Scalar size does not establish parameter-gradient dominance: the selected three-forward/three-gradient diagnostic measures that separately. [Saved calculation and pins](saved_scale_report.json).

The proposed next learner experiment should therefore supervise **all six physical state groups**, rebuild pure DirectFeatures on each perturbed state, and preserve nominal targets plus coupled physical-response anchors. The selected data design now declares radii of 0.001 m root position, 0.01 rad root rotation and joint position, 0.05 m/s root linear velocity, 0.25 rad/s root angular velocity, and 0.01 native joint-speed cap. Its smallest saved joint-position margin is 0.015557 rad, so no shrinking or dropping is needed; a violated static/chart/feature check stops generation. Retain exact quaternion/difference conventions, both teacher clips and all clipping/zero-gain counts. The selected loss is target-span-normalized endpoint-minus-center MSE averaged across 54 dataset/phase/state-group cells. With different physical radii, this is balanced finite-feedback matching over declared neighborhoods, not a common-unit Jacobian norm. No hidden division by per-group radius is implied.

This is feedback distillation, but calling the teacher *locally stabilizing* remains premature. The saved maps are stale committed plans; 41 of 42 actual-state fixed-map responses hit the feedback clip. They do not certify replanning behavior or recovery from the new states. If full-state probes expose incompatible or saturated supervision, the appropriate next data source is a bounded actual-state MPC replan with native feasibility checks, not gain sign edits or more iterations on the same labels.

The existing MPC has completed the full 1,569-control simulation, 819 source controls and separate 250 hold, but is not yet a fast runtime replacement. Its qualified query250 nominal run took 2,664 s; a fresh 30-control BFM seed alone costs 288.7/383.9 ms median/p95. The private bulk component's 100 ms constant-target forecast takes 5.63/6.85/9.50 ms median/p95/max, including restoration, checks and copy. That excludes MPC derivatives, optimization, seed generation and asynchronous activation; four forecasts plus policy are not 20 ms qualified. [Measured bulk scope](../native_bulk_forecast_v2/README.md). Fast MPC is a separate performance/integration project, not a controller fix established by these timings.

[Sobolev Training](https://arxiv.org/abs/1706.04859) and [TaSIL](https://arxiv.org/abs/2205.14812) support derivative-sensitive imitation. TaSIL's trajectory guarantee assumes an incrementally input-to-state-stable expert, which is not established here. [Tube-guided MPC augmentation](https://arxiv.org/abs/2306.00286) uses robust tube structure absent from this setup. These motivate the experiment; they do not supply a stability guarantee for clipped contact dynamics.

The selected diagnostic completed exactly three FP32 training-model forwards (14,110 rows) and three separate loss gradients, with no optimizer or native calls and unchanged model parameters. Nominal/velocity/physical parameter-gradient norms are 0.00266348 / 0.000372847 / 0.000521267. The original total gradient is closely aligned with nominal (cosine 0.96984). A 10,000 velocity weight makes its gradient contribution 1,399.85 times nominal and points locally uphill on nominal. Root's independent saved-vector audit finds that weight 7.14363 equalizes nominal and existing-velocity gradient norms and lies inside this batch's common-descent interval [6.11107, 224.27702]. This is local Euclidean gradient geometry, not an AdamW finite-step or trajectory guarantee. [Independent gradient evidence](../direct_target_gradient_saved_review_v1/report.json).

Do not transfer 7.14363 to new full-state data. The selected next design measures nominal/full-state/physical gradients once on its fixed initial schedule, sets the full-state coefficient to `||g_nominal|| / ||g_full_state||`, and then freezes it. It retains all nominal/physical anchors and records geometry rather than claiming future common descent. Fresh AdamW and a fixed budget separate that changed objective from the failed continuation. Actual fitting remains gated on full-state data qualification and concrete source review.

The saved analysis itself made zero model, optimizer or native calls. The separately authorized gradient run made only the stated three forwards/gradients; its first launcher failed before Python creation and consumed zero task calls, and the repaired launcher completed with exit 0. All original failed trajectories and release artifacts remain preserved.
