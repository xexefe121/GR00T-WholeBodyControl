The strongest next learner change is explicit, properly scaled feedback supervision, keeping the current direct architecture and nominal anchors. Another unchanged-objective fit or broad nominal collection has weaker support. Do not choose the response coefficient from scalar losses alone: the single fixed-batch parameter-gradient diagnostic is prepared, and the independent 42-state committed-map decomposition is pending. Neither is another controller candidate.

The new, numerically qualified FP64 implementation failed at control291/substep6, 5.832 s, with native speed ratio1.11995; it reached zero source controls. Its first target at the exact shared control250 state has 0.045540 rad RMS error, improved from direct5000's0.098651. Nevertheless control251 error rises to0.093250 with0.417523 rad/s joint-velocity departure, and control252 error reaches0.305625 with1.010634 rad/s departure. First clipping is256;34 of42 learned commands clip. This onset precedes clipping and survives removal of direct prior/BFM dependencies. These are saved different-state comparisons after250, not expert predictions from those actual states. See [semantic review](../direct_target_fp64_saved_semantics_review_v1/report.json).

Phase balancing is already substantial. Acquisition, source and return each receive one third of nominal loss mass; each of the five datasets receives one fifth. The three walk003 datasets therefore receive60% of nominal mass and100% of velocity/physical-response supervision. A query250 acquisition row receives8.19 times the weight of a query250 source row, and57.8 times a PICO source row. More acquisition weight may improve the initial command, but lack of acquisition rows is not established as the cause of the growing closed-loop departure. All9904 nominal rows remain useful; removing difficult rows would obscure the test.

The direct1000 inputs already contain current joint position/velocity, root angular and heading-frame linear velocity, projected gravity, root height, and eight future full-body goal samples. Removing BFM base and prior feedback removed a problematic path but did not provide a stabilizing structure. The existing native PD damping remains active; the learned target can still create destabilizing effective velocity feedback. A diagonal sign constraint would discard valid coupled balance behavior and is not justified by these results.

For normalized target z=(target−default)/span and normalized joint speed r=dq/native_cap, the current velocity error for each sign is

    e± = [f(x±0.01 axis)−f(x)] − [z_teacher(x±)−z_teacher(x)].

Its existing loss averages e±². The signed finite-secant error is e±/(±0.01), so its squared loss is exactly10000 times the current loss. A2500 factor would correspond to a different positive-minus-negative central difference. Both teacher clips are included; these labels are finite secants of an already committed plan, not an analytic Jacobian or a replanned expert.

Saved complete-corpus results, retaining equal nine-cell weighting:

| Quantity | Direct5000 | Current55000/FP64 |
|---|---:|---:|
| Nominal normalized MSE |0.00146922|0.000448775|
| Physical response MSE |0.000193443|0.0000956462|
| Velocity finite-secant MSE |0.555893|0.680371|
| Same teacher secant energy: zero-response baseline |0.334851|0.334851|

All nine velocity cells are worse than the zero-response baseline.98.48% of the current signed-chord error is its odd/slope component. Across the108013 probes with neither feedback nor native teacher clipping, the raw-row-weighted secant error is0.531463 versus zero-response error0.135133. Thus clipped-teacher ambiguity alone does not explain the poor learned response. Conversely, blindly multiplying by10000 would make the velocity term99.920% of the current scalar objective; that is not proof of its parameter-gradient share. See [saved scale evidence](saved_scale_report.json).

Velocity-only correction is also incomplete coverage: it probes23 of the58 tangent directions. Root pose/rotation, joint pose and root linear/angular velocity comprise the other35. Each physical label supplies one coupled20ms displacement generated by the earlier70000 policy, not an independent basis at the new actual states. No present certificate says that these local committed maps stabilize multi-step departures, and no current data provide replanning labels at the new55000 state distribution. The pending saved K decomposition can show which missing directions contribute to this failure; it cannot certify an updated controller.

Provisional concrete experiment: replace the tiny-displacement feedback penalty with a dimensionless finite-secant objective, retain nominal and physical anchors, and freeze one coefficient only after inspecting the selected fixed-batch gradient norms/cosines. If the new state decomposition shows major unsupervised pose/root contributions, extend the same bounded, physically scaled finite-secant construction to those state groups before fitting, rather than pretending more velocity weight covers them. Do not use unbounded raw K, sign edits, per-sample normalization by tiny responses, or discard clipping/zero-gain rows. Existing evidence does not yet choose the coefficient or authorize that fit.

Explicit locally stabilizing action-feedback distillation is the longer-term target, but requires a trustworthy local feedback teacher around actual deviations. Calling the current clipped, occasionally zero/extreme committed gains “stabilizing” would overstate the evidence. Full-state perturbation labels or fresh actual-state MPC replanning would be distinct data work with fixed budgets and native feasibility checks, not a reinterpretation of existing nominal labels.

The existing MPC remains the demonstrated route through this full simulation: the query250 branch completed1569 controls,819 source controls and its separate250 hold with independent native/intent checks. It is not a measured fast controller. That nominal run took2664 s; its fresh30-control BFM seed alone costs288.7 ms median,383.9 ms p95 per planning event. The separately qualified bulk forecast takes5.63/6.85/9.50 ms median/p95/max for a single100ms constant target, including restoration, scalar checks and copy. Those numbers exclude MPC derivatives, optimization, seed generation, cost ranking and an asynchronous activation schedule. Four forecasts plus policy are not20ms-qualified, and a faster forecast does not fix a feedforward learner's feedback structure. See [bulk timing](../native_bulk_forecast_v2/README.md). Fast MPC is a separate integration/performance project, not a ready replacement claimed from those timings.

Derivative matching has relevant precedent in [Sobolev Training](https://arxiv.org/abs/1706.04859) and [TaSIL](https://arxiv.org/abs/2205.14812). TaSIL's trajectory guarantee assumes an incrementally input-to-state-stable expert; that assumption is not established for this clipped/contact/committed-plan setup. [Tube-guided MPC augmentation](https://arxiv.org/abs/2306.00286) uses robust tube-MPC structure absent here. These works support testing feedback-sensitive supervision, not claiming a stability guarantee.

No model, optimizer or native calls were made by the saved analysis. The gradient source is prepared only; final recommendation awaits that bounded result and the independent full-state decomposition. Original failed exports and all failed trajectories remain preserved.
