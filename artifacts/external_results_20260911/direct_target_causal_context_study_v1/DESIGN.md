# Matched causal-context utility study — source preparation

Prepare two fixed 3000-update conditions from the same ordinary65000 checkpoint. Both use a 1323→256 ELU→256 ELU→23 linear head: the current1000 state/goal features followed by previous_action23 and incoming history300. Copy the old first-layer1000 columns and every other weight/bias exactly; initialize all323 added columns to zero. One condition receives true recorded causal context. The other receives normalized context zero. No task training, inference or dynamics is authorized by this preparation document.

The control measures the utility of adding these context inputs under this training protocol. A difference does not uniquely prove hidden-state causation or separate information from effective input capacity. If both improve similarly, this experiment does not establish a need for context. If response improves while nominal/physical fit degrades, retain that tradeoff; do not redefine success from a chosen scalar or run more updates.

## Evidence and limits

The independent native replay reproduced all2969 samples of the final65000 failure; right-knee speed exceeded20rad/s at control296/substep9. Saved semantics show first departure251, first clipping257, and correct applied-prior/history arithmetic. The current fit's full-state response loss fell40.56%, but remained1.13813 times the zero-response baseline; joint-velocity response worsened. Nominal and physical aggregate improvements were0.60% and1.17%. Eleven of15 nominal cells,48 of54 response cells and7 of9 physical cells improved.

The last two1000-update sampled response averages differ by only−0.27%; different sampled pairs and a declining learning rate prevent a convergence claim. Initial nominal/response gradient cosine was−0.0333 and all three combined-gradient descent dots were positive. No final or per-step separate gradients were saved, so loss interference is not established. A fresh AdamW reset at1e−4 produced an89.295× nominal-loss spike at update2, despite gradient norm below the clipping threshold; the matched new conditions use the same lower fixed1e−5→1e−6 rate.

Holding the previous target is21.95 times worse than the current model as an output baseline. That result does not test whether a learned policy benefits from prior/history inputs. All9904 nominal rows retain previous_action23/history300. Full58 probes can reuse their matched center context; the3054 physical endpoints retain advanced_history and actual-normalized applied action. This makes a small matched context test possible without new labels, model probes or dynamics. The receipt for these saved findings is `../direct_target_full_state_followup_analysis_v1/report.json`, SHA256 `15585ddf96f856efb6cc6046cc5341173a5afaf7db3f73416d212705ba511661`.

## Exact data contract

- Nominal: append each actual row's previous_action23, then actor incoming history300, in that order. Preserve five datasets and the existing15 phase cells; do not infer context from current targets.
- Full58: each of116 endpoints per center uses that center's unchanged prior/history. Current-state perturbations must not rewrite lagged history. All354612 original state/goal vectors and targets remain unchanged.
- Physical: append policy_actual_normalized_action23 and advanced_history300. Independently reconstruct the prior from the saved actually applied target using the original float64 inverse-PD expression followed by float32 cast. Do not substitute outgoing_raw_prior. The nominal successor in the paired loss uses its own recorded nominal context.
- BFMHistory returns the four previous samples before shifting in current terms. Its newest action is one command older than the explicit previous_action. Preserve this timing and alphabetically flattened term order; no unqualified lag reindexing.
- Keep every original1000 normalization mean/std byte-exact. Compute only the323 new coordinates' equal15-cell float64 mean and variance about the global mean, cast moments to float32, and floor std at0.05. Both conditions use exactly the same resulting1323 normalization.
- Blinded input uses the stored float32 context mean in raw input columns1000:1323, making the normalized context exactly zero under both float32 and promoted float64 arithmetic. The true condition uses the actual finite recorded context. Save the condition explicitly; do not silently alter runtime inputs.

## Fixed paired training

Run `blinded` then `causal` in fixed order. Each starts from identical expanded weights, identical restored65000 RNG, and fresh empty AdamW moments. No condition inherits the other condition's optimizer/RNG/output. AdamW uses foreach=False/fused=False, weight_decay1e−5, gradient clip10, deterministic CUDA, no AMP/TF32, and CUBLAS_WORKSPACE_CONFIG=:4096:8. Each performs exactly3000 updates with inclusive cosine1e−5→1e−6; ordinary endpoint is68000, fresh optimizer step3000. Preserve both endpoints even if one is worse.

Reuse the exact first3000 rows of the already saved65000 schedule_centers/schedule_axes, byte for byte, for both conditions. Each update evaluates all9904 nominal anchors,1728 full-state endpoints from864 fixed pairs, and all3054 physical endpoints. Reuse live nominal center predictions. The original loss arithmetic and cell denominators remain unchanged: nominal15-cell mean + **1.8188207859141674** × full-state54-cell mean + physical9-cell mean. No gradient calibration, radius division, loss reweighting, label filtering or sampling draws.

Per condition:9000 training forwards,44,058,000 training rows,3000 backward/update calls. Total:18,000 training forwards and **88,116,000 training rows**. There are zero separate calibration forwards or gradients. Count attempted/returned/synchronized/verified calls and every actual optimizer step; failures retain returned outputs, partial losses and an explicitly nonresumable state.

## Fixed diagnostics and release

For each condition, run the existing complete nominal/full58/physical partitions (9904/354612/3054, batch256,1437 calls per complete pass): initialGPU32, finalGPU32, finalCPU64, finalGPU64 and finalORT64. This totals2,940,560 Torch diagnostic rows/11,496 Torch diagnostic calls and735,140 ORT rows/2874 ORT calls for the pair. No extra baseline forward is needed: compare initial outputs against saved65000 outputs and the other condition's saved initial arrays. Changed first-layer MatMul dimensions can change float32 accumulation; report every initial preclamp drift and gate at1e−5rad, without claiming byte identity.

Save ordinary endpoint PT before exporting. Manually export the exact finalfloat32 weights/normalization promoted to float64, bounded-Exp/Where ELU, then cast public normalized output tofloat32. Runtime reconstructs default_f64 + promoted_original_span_f64 × head_f32 and applies unchanged nativefloat64 bounds. Keep the original1e−5rad CPU64/GPU64/ORT64 preclamp gate. Record all cell metrics, first24-phase metrics, paired odd/even/zero-response comparisons, clipping and nine backend drift arrays per condition. These are fixed final diagnostics, not a checkpoint-selection procedure.

No controller trial or witness is selected here. Report both ordinary endpoints and all counterevidence. Any later deployment decision must be separate, with one predeclared endpoint, measured whole-policy batch1 time below20ms, exact extended query250 context/witness gates, original BFM250 prefix, full1569 lifecycle plus conditional continuous250 hold, and unchanged independent native physics/intent acceptance. Added first-layer work is82,688 multiply-accumulates, about25.25% of the old head's dense-layer work; total latency is unmeasured. This study provides no asynchronous real-time or hardware qualification.
