# Saved balanced-response continuation audit

Preparation only. Target is the corrected `direct_target_causal_response_balanced_student_v2` single causal continuation. The failed v1 attempt remains preserved and is ineligible: the request writer requires completed optimization and all final diagnostics before it can create an actual audit request. No actual audit request or fit-evidence audit has run during preparation.

The auditor reuses five byte-identical qualified independent helpers for context, complete full-state metrics, original nominal/physical arithmetic, restoration comparison and the 20-node FP64 export graph. New independent math reconstructs six teacher-energy weights, original and balanced 54-cell losses, all 3,000 saved update ledgers, warm actor/AdamW/RNG identity, and the balanced diagnostics from each of five complete saved prediction sets. It retains the original nine 1e-5-radian FP64 parity gates and separately records evidence validity and numerical qualification.

The full saved audit checks 9,904 nominal anchors, 354,612 signed full-state endpoints, 3,054 physical endpoints, their actual contexts and copied normalization, all 3,000 x 864 schedule entries, 9,000 training calls / 44,058,000 rows, and all 7,185 diagnostic call partitions. It neither evaluates a task model nor reconstructs gradients or optimizer updates. Full optimizer moments are checked at restoration; final moment schema and all six step-6,000 counters are checked without claiming that saved cells prove the authenticity of training gradients.

Metadata follows the actual single-fit producer: `dict(request, condition=...)` intentionally replaces existing metadata keys; the concrete root review exposes request, frozen-receipt and launcher hashes at top level. The process directory is `fit_process_v1`; the owner is `owner_completion_verification.json`. All 16 release roles are literal path/hash subjects, including the corrected full-state data owner and new energy source. Current and pre/post launch pin membership, PID relations, known exits and owner accounting must agree.

39 synthetic tests passed. Eight isolate request/review AST paths, including the old duplicate-key failure and independent corruption of all three concrete hashes. The remaining tests cover warm-state corruption, group weighting/zero-response energy, dtype-sensitive ledgers, context chronology, applied physical prior, schedule, metrics and graph corruption. No task arrays, task checkpoint, task forward, gradient, optimizer, ORT or native operation was executed by this preparation.

An actual completed producer may have failed the finite numerical parity gate. That result is retained as `evidence_audit_passed=true`, `export_qualified=false`, with the original failed report and failure capsule required. Missing/incomplete optimization or diagnostics is not eligible. Auditor exceptions are preserved and cannot trigger an automatic retry.
