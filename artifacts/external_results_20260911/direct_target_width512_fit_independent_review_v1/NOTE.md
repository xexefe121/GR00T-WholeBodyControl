Width512 trainer source review: PASS for the exact28-source preparation. Concrete request, runtime and launch selection remain root-owned.

The21 unchanged modules retain data/feature/context normalization, nominal15/full-state54/physical9 objectives, fixed coefficient and energy weights, diagnostic partitions, target/span conventions, numerical release checks and failure helpers. Seven changed/new modules were read independently: response_contract.py, response_data.py, response_support.py, train_response_balanced.py, width512.py, width_restore.py and width512_promoted.py.

Warm initialization preserves all six old256 actor blocks and AdamW moment blocks, the entire parameter group, normalization and source RNG. Added incoming neurons use one fixed local CPU generator. Added outgoing paths and new moment entries start zero; each expanded tensor retains its step6000, including the new entries. This is an explicit expansion convention, not a claim of an unchanged optimizer trajectory after new paths open. The model preserves old contraction shapes at initialization; additions retain signed-zero bytes while admitting gradients to new outgoing blocks.

The driver verifies actual loaded actor, optimizer and RNG before its first forward. All367570 initialGPU32 rows retain the1e-5rad admission threshold. Byte equality is reported independently and is not a second hard gate. A failed initial gate causes zero updates. Ordinary71000→81000 and AdamW6000→16000 require10000 updates, the complete saved10000×864 schedule, exactly30000 training model invocations/146860000 rows, and no new gradient calibration. Rate indices0..249 ramp inclusively1e-6→1e-5;250..9999 decay inclusively1e-5→1e-6. The two adjacent peak rates are intentional endpoint conventions.

Partial optimizer execution preserves attempted/returned/synchronized flags and actual six step counters. A step or synchronization failure does not commit a schedule row or loss row. Failed state and current-call outputs remain evidence; no automatic retry or resumable claim is added.

The FP64 export uses every512 parameter block and owned exact promotions, including newly opened paths. Public input/output remain float32. CPU64/GPU64/ORT64 nine corpus comparisons retain the original1e-5rad gate. FinalGPU32-to-promoted drift remains diagnostic, with no added acceptance threshold. Same-width mathematical equivalence is distinct from floating-point reduction identity; actual selected data gates remain necessary.

Independent tests:18 synthetic integration cases plus one additional FP64 opened-path/buffer-ownership mutation case. They exercise coherent capsule corruption, restored group/normalization/RNG, initialization threshold/nonfinite behavior, schedule/rate/counts, actual optimizer-transition AST under injected partial-step/synchronization failures, and preserved parity source. All19 pass. Three synthetic CPU model forwards occurred only in the FP64 mutation test; no actual checkpoint/dataset loads, task forwards, gradients, optimizer updates, CUDA, ORT or native calls occurred. The final source snapshot matches the recorded tested draft map exactly.

The completed saved schedule proof and prior initializer/exporter source reviews are consumed as existing evidence without repeating their actual data calculations. Legacy test_response_continuation.py remains a preserved source file and is not claimed as a newly passing width512 test suite. The wrong-host PowerShell7 guard attempt is preserved by the owner; selected PowerShell5.1 guard/AST checks pass. This source review does not qualify controller stability, latency or a future81000 endpoint.
