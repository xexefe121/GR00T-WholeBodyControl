# Width512 warm endpoint schema

Preparation only. No fit, checkpoint load or actual inference has run here. Runtime qualification and concrete root clearance remain required.

The ordinary endpoint is `fit/student_head.pt` at81000, optimizer16000 after10000 updates from71000/6000. All six actor tensors remain float32, with shapes512×1323,512,512×512,512,23×512,23. `student_head.onnx` keeps float32[batch,1323] input and float32[batch,23] normalized absolute-target output; normalization, linear layers and ELU execute internally in float64. The existing span/default/native-clamp contract remains unchanged.

`fit/initialization.pt` contains actual expanded `actor_state`, complete expanded `optimizer_state`, `rng_after_restoration`, unchanged `feature_mean`/`feature_std`, `condition=causal`, `ordinary_start_step=71000`, `optimizer_start_step=6000`, `hidden_width=512`, original checkpoint hash, `expansion_seed=20260912` and `expansion_generator_state`. The source RNG is retained separately from the local expansion generator. New entries in all moment tensors are zero and share the original scalar per-tensor step6000. The initializer does not claim continuation of the old optimizer trajectory after the new paths open.

`fit/restoration.json` records exact old actor/moment blocks, exact expanded actor/optimizer, unchanged full group and normalization/RNG, zero new moment/outgoing blocks, retained step6000,512 geometry and ramp/cosine proposal. `initial_drift` compares all367570 initialGPU32 outputs against saved71000 finalGPU32 in the same1437 partitions. It records per-corpus byte equality, maximum/RMS preclamp error and the unchanged<=1e-5rad gate. A subthreshold non-byte difference is disclosed; no hidden byte-only admission rule exists.

`fit/shared` retains original causal context, data identities and1323 normalization byte-for-byte. Prior3000-row schedule files become `source_schedule_centers.npy` and `source_schedule_axes.npy`; the actual10000-row schedule is copied as `schedule_centers.npy` and `schedule_axes.npy`. `source_output_manifest.json` remains the prior context manifest. `schedule_lineage.json` binds full/prior schedule hashes, exact first3000 agreement and `schedule_generated=false`. No source labels, contexts, normalization or random samples are regenerated.

`used_centers.npy` is int32[10000,864]; `used_axes.npy` is int8[10000,864]. Uncommitted entries remain-1 on failure. `training_progress.npy` is float64[completed_updates,6], with columns nominal,balanced_full_state,physical,balanced_total,learning_rate,preclip_gradient_norm. Original nominal15/full_state54/physical9 cell losses remain separate float64 arrays. `balanced_full_state_cell_losses.npy` preserves weighted54 columns and `original_objectives.npy` original full-state/total values. There is no added gradient calibration or loss-weight update.

Five diagnostic labels remain `initial_GPU32`, `final_GPU32`, `CPU64`, `GPU64`, `ORT64`. Each has float32 predictions for nominal9904×23,full_state354612×23,physical3054×23, and a complete metrics JSON with original and balanced54 cells, group comparisons, clipping and zero-response diagnostics. All1437 partitions/backend are preserved in `diagnostic_calls.jsonl`. Nine CPU64/GPU64/ORT64 corpus-pair comparisons retain the original1e-5rad release threshold. Nine finalGPU32-to-promoted-backend drift arrays and clipping summaries remain diagnostic, without introducing another admission threshold.

Counters refer to outer model forward invocations, not individual internal `F.linear` contractions. Training30000 forwards/146860000 rows; Torch diagnostics5748 forwards/1470280 rows; ORT1437 calls/367570 rows; calibration0/native0/BFM0/manual-export-trace0. SplitGPU32 uses multiple contractions per outer call. All counters retain attempted/returned/synchronized/verified distinctions and all failed current-call arrays, committed prefixes and failed-state snapshots are preserved.

Final `report.json` retains the prior report fields with81000/16000/10000, adds `architecture=[1323,512,512,23]`, `hidden_width=512`, `expansion_seed`, and records `training_first_layer_execution=split_old256_new256_original1000_plus323`. `export_first_layer_execution=monolithic_float64_1323` remains. `output_manifest.json` binds actual saved outputs; existing raw/process/diagnostic-exit separation and ordinary owner16 release roles remain, with width qualification/schedule provenance carried through request subjects.

Actual fit source, runtime, schedule proof and promoted exporter tests must receive review before a concrete clearance. No controller witness/canonical or <20ms end-to-end qualification follows from source preparation.
