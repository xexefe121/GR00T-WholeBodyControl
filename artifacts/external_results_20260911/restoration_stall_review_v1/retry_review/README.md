**Read-only review passes for the narrow opt-in K=0 retry.** This is source/contract validation, not a completed-continuation result. `source_binding.json` hashes five reviewed sources and preserves copies. The main iLQR core and main tracker are byte-identical to the failed-search snapshots. Restoration residual and target-reference methods are AST-identical; the new option defaults to false.

- The restoration subclass replaces only the third item of the private rollout gains tuple with `zeros_like(K)`. Caller k, K and nominal states are not modified. The main tracker does not implement this option; actual feedback correction remains clipped to ±0.1 rad as before.
- The runner always attempts guided restoration first. If neither required certificate admits its final proposal, the opt-in path attempts K=0 once. Both attempts start with `actual` and a fresh `warm.copy()`; `original_targets` remains the original shifted warm seed throughout. The failed guided result is not used as the retry's initial controls.
- Each attempt emits its normal final optimizer proposal. There is no selection among hindsight-certified intermediate alpha candidates. Each final proposal must pass the main every-2-ms hard nominal rollout and the independent 300-step full actual-MjData native oracle. Only then can it seed the main hard tracking solve, which rerolls it again.
- Both attempt proposals bind the actual integration state, history, previous action and original warm target array in separate NPZ files with SHA256. Mode, merit contract and elapsed time are recorded separately. The flag is reset after the restoration block. None of this advances actual physics or history.
- Prefix comparison now happens exactly at the supplied prefix length before further planning, so the 65-control original restoration continuation can be compared before a control-3805 retry. The requested 100 controls remains unchanged. Full versus attempted/partial control counts, final source time from actual physics steps, raw-before-parity failure retention and independently accumulated expected clock are now present.

The new focused test checks default numerical equivalence to the base rollout, exact K=0 feedforward target algebra, caller-K immutability and isolation from the main tracker. The reviewer read but did not execute it. It does not separately exercise the runner's guided-pass/skip-retry and failed-hard-oracle/retry branches; those orderings were inspected directly in source. Subsequent prefix/continuation evidence should retain the exact mode, both certificates and actual final counts rather than treating this review as a physics pass.
