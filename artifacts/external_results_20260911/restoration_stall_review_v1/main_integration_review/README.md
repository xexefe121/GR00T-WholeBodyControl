Read-only review: shared `restore_feasible_seed` and new opt-in main evaluator integration pass the inspected contracts. Four source snapshots/hashes are retained here; reviewer ran no solves, physics or tests.

The main evaluator copies the original shifted warm commands before selecting among shifted/recorded/fresh seeds. Restoration runs only when all available standard choices lack a finite hard-feasible cost. Both attempts use that original copy and the unchanged actual state; guided restoration precedes at most one K=0 retry. The helper's return tuple binds the same hard-checked states, cost and target array and requires the independent full actual-MjData native oracle. The main solver immediately rerolls the accepted seed under hard feasibility.

Each attempt saves targets, original warm commands, complete actual integration state/specification, control number, request SHA and available BFM history/action fields. Events are written before the following main solve, including cases that fail without executing a control. Planning and certification do not advance actual state/history. No original source timing, requested/full lifecycle count, physical PD law or existing actual-step guard is changed.

Flag-off control flow bypasses the new helper. The opt-in requires hard feasibility and H30. The focused known-3770/3805 test checks guided-first/zero-retry ordering, exact returned target parity with frozen certified proposals, matching cached states/cost, unchanged warm input and unchanged actual full integration state. The reviewer inspected that test; execution/result belongs to the implementation agent.
