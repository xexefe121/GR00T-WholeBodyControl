# Independent saved clock audit

Source preparation only. No actual clock benchmark, worker, MuJoCo, model, optimizer or saved task-array evaluation was run by this preparation. Forty synthetic tests passed under Windows Python 3.10 / NumPy 1.23.5. The eventual saved audit requires the existing WSL Python 3.11 / NumPy 1.26.4 runtime, a literal review of every auditor source hash, explicit parent selection, completed owner accounting and an immutable request. No actual audit request has been created.

The original four root math/test files remain unchanged. `source_audit_v1` copies and extends the two independent math helpers, retains their synthetic checks, and adds independent canonical protocol, counter, process-owner and output verification. `qualified_binary_math.py` is the unchanged independently reviewed decoder from the earlier native stepper saved audit; the producer's decoder and controller helpers are not imported by the audit. The single observation implementation import occurs only in an existing synthetic rotation test.

The audit separates evidence integrity from physical, timing, command and component qualification. A correctly preserved partial failure can pass evidence integrity while every full-run qualification remains false. Requested scope always remains 1,819 controls / 18,190 native steps, including the original 1,569-control lifecycle and continuous 250-control hold. It makes no online-policy, teleoperation or hardware claim.

Checks include all actual packed 373-float captures and full 291-value integration overlap; independent manual PD and repeated 2 ms simulation clock; the original strict native limits and initial step-zero force exception; source samples, full integration boundaries, targets, raw actions and histories; independent measured terms and each single history update; exact issued canonical job bytes and source-window identities; plant-copy completion before authoritative admission; rejection order, deadlines, held-action latch and retained nominal source gaps; worker input/output hashes, receipt timestamps, process identity and actual publication status; all four persisted MJB buffers against the already qualified expected bytes; current launch/input/output hashes and owner/start/exit/absence lineage.

Final uncommitted native captures and reserved control records are checked from owned raw evidence without adding committed credit. The audit retains unknown native mutation and failed-start descendant uncertainty. A failed uncommitted cycle can lack an exact wake/end sample; its retained aggregate debt is reported separately and cannot earn full timing credit. OS-killed jobs lacking the required completed preservation/owner packet cannot pass this audit. No lost sample or missing response is reconstructed as an executed command.

After parent source review and a selected completed clock run, `source_audit_v1/prepare_request.py` can freeze the actual original request, launch/clearance/review, owner and process records, every actual output, the qualified original expert traces/fixture/reference/native contract/MJB and all auditor sources. Its `--root-selected` argument records parent authorization, not a bypass. It requires a review with `source_sha256` keyed by all relative Python filenames. `audit_clock.py --request ... --output ...` then reads only those frozen files. The output directory must be new; comparisons, first mismatch and final report are preserved. Every input and the request are rehashed before successful completion.

No execution selected or started by this document.
