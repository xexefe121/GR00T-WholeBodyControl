Saved gains support diagnostics and perturbation-direction proposals. Do not use them directly as residual-head derivative labels or treat arbitrary perturbed states as qualified demonstrations.

All1,268 executed fresh-expert commands reconstruct bit-exact from254 frozen plans and actual states. Actual feedback correction never reaches its0.1rad cap (max0.0359852rad); native target clipping affects11 components. Nonetheless, maximum joint-position gain is43,165.2rad/rad. For16 controls, some1mrad joint-position perturbation alone would make the linear correction exceed0.1rad. One plan(control706) accepts no update and returns zero gains for5 controls. That does not establish zero sensitivity of a fresh MPC solve. Mixed-unit overall gain max80,802 is recorded only as a diagnostic, not a physical derivative norm. None of these perturbations was simulated.

The frozen difference is actual minus nominal. Tangent58 order: root position0:3, right-multiplicative body orientation3:6, joint position6:29; then raw velocity difference29:58 (root linear29:32, free angular32:35, joint35:58). Core difference uses conjugate(q_nominal)*q_actual; local quaternion integration uses q_nominal*exp(delta_theta). Backward K already contains the negative solve sign. The command is native_clip(u_plan + clip(K*difference,-0.1,0.1)); its local derivative includes both clipping active sets, and is undefined at their boundaries. Reversing the error or adding another minus sign is wrong.

K is the last accepted rollout's generating backward sweep, computed around the previous incumbent. It is not a fresh derivative of the returned receding-horizon optimizer. No accepted update yields K=0. Finite-difference contacts, seed selection, hard-feasibility lane acceptance, constraint activation and regularization changes can make the replanning map discontinuous. A frozen K test also retains its nominal trajectory, shifted30-target warm start and local position within a5-control commitment. Those planner inputs are absent from the1069 student features; BFM output and prior action do not encode that entire state.

The learned output is a residual to a state/history-dependent BFM base. Even a valid applied-command Jacobian cannot be copied to the residual network: subtract the BFM-base derivative, apply the feature/normalization/span chain rule, and honor preclip residual versus applied-target semantics. Saved K alone supplies none of these missing derivatives. Do not add planner time, saved gains or warm targets as covert deployment inputs.

If a later bounded perturbation experiment is authorized, declare physical-state versus observation-noise perturbations separately. Preserve full integration state, global clock, named history and previous action at the actual decision boundary; use correct quaternion tangents. A physical perturbation changes current state but cannot rewrite prior sensor history. Observation noise must not change the plant. Recompute current features/base from that declared state/history, preserve reference and replan fresh MPC. Record every native2ms substep through committed controls with original gates. A safe600ms candidate certifies only that local candidate. Admission under the current expert-label contract still needs an independently reproduced uninterrupted remainder of source/return plus quiet hold from the perturbed state, without resetting or splicing a nominal tail. Reject and preserve unsuccessful branches.

Actual-state DAgger already has stronger evidence: the query starts from the state/history truly reached by final20000 student; fresh MPC then passes all1,569 lifecycle controls and250 continuous hold controls under independent physics/intent/quiet audits. Its1,268 labels have no exact target/residual conflicts. If the new aggregate student fails, inspect its actual state and label error before selecting another experiment. Another explicit actual-state query is better supported than unconditional K-gradient supervision, although it does not by itself solve fit error or hidden-state ambiguity.

Earlier saved-plan/K full-walk002 perturbation replay accepted0/17 cases; the old short dataset accepted only4/17 and lacked full-source qualification. This supports retaining the stronger fresh-replanning requirement. Native PD target saturation is separately ruled out as the main ambiguity: fully saturated control/joint components are about0.2%, and changing a target also changes BFM action history. Do not average, clip or delete qualified labels based on saturation speculation.

Evidence: metrics.json/per_control.json independently reconstruct saved commands; input_hashes.json binds all plans, native limits, source and prior evidence. inspect_saved_gains_v2.py imports only NumPy/stdlib and AST-extracts three pure quaternion/difference functions. Original writer failed before producing metrics because physical motor actuator_ctrlrange is zero; corrected version uses the target planner's frozen native joint_limits. Failed source and explanation remain preserved. No actor inference, optimizer, physics, label change or hardware action performed.

Source anchors: run_actual_student_oracle.py:302 (warm shift), :305 (fresh solve), :313 (applied correction); g1_true23_mjbatch_ilqr_core.py:81 (difference), :240 (K sign), :313 (initial zero K), :336 (accepted gains); student_linear_runtime.py:68 (1069 feature construction), :76 (history-dependent BFM base), :101 (history/command arithmetic). All paths bound in input_hashes.json.
