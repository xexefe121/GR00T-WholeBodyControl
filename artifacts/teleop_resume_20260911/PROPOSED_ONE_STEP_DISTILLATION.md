# Proposed one-control physical branch data

Status: collection stage selected by root, 2026-09-11 09:08 UTC. Implementation and source review remain pending. No fit or canonical trial selected. The missing old-state reconstruction and 60-call previous-action diagnostic are selected independently. Collection may run once only after the snapshot prerequisite and frozen source review pass.

Saved-only feasibility is recorded in `E:/codex-artifacts/sonic23_teleop_resume_20260911/previous_command_perturbation_feasibility_v1/behavior_report.json`, SHA256 `f6c0ac3f60ed1b9776e30aefb5ddbcf48b507ff2eefa15e21368d0ed49b0c0d4`. The saved first query250 delta, target, outgoing raw prior and shifted history happen to match the actual WSL run exactly. Its actual control251 full state and all ten intervening samples therefore form a fixed, independent branch witness. This remains unexecuted physics. Across the 3,054 saved nominal predictions, 1,459 commands clip native targets and 610 successor labels are original replan boundaries; neither group may be silently removed.

The velocity-response head failed at control 261 despite better saved-input errors. Its first actual departure changes pose, root motion, joint velocity and previous action together. Independent axis probes do not supply those coupled physical states. A small collection of branches driven by the frozen head's own commands could cover the next measured state it actually produces.

Use all three existing walk003 expert datasets. For every nominal precontrol c from 250 through 1267 inclusive, start a private native instance from its complete 291-value state and warning ledger. There are 3,054 predetermined branches. The label clock is c+1, covering 251 through 1268. Original control 250 remains in the unchanged nominal anchors. Control 1268 is not a branch start because its successor is the separate terminal controller. No source frame is cropped from a canonical evaluation.

Before the branch at each row, replay the original nominal command for exactly ten native 2 ms steps. Compare all saved states, command torques, actual actuator forces, accumulated clocks, warnings and the complete successor integration state. Then independently restore the original start for the student's branch. This is at most 61,080 private steps including nominal checks; the separately selected old-state reconstruction is additional.

The final update-70,000 ONNX head is frozen. Its existing Windows batch predictions are diagnostic evidence, not assumed identical to the WSL batch-1 deployment output. A selected collector should run the original deployment head once on the immutable nominal feature input, preserve its raw output, form the original unclipped total proposal, and apply the original native target clip. Query250 control250 must match the independently bound activation witness before its first branch step. No command blending, gain injection, target smoothing, phase lookup or adaptive perturbation scale is added.

Advance the BFM history exactly once from the original measured state and incoming previous action at c. The outgoing raw combined action becomes the separate previous action at c+1, exactly matching the unchanged learned runtime, including any difference from the normalized applied target after clipping. The newest history action still describes command c-1. Do not overwrite it with command c. This gives physically consistent plant state and internally consistent policy memory.

At a complete feasible branch endpoint, recompute the original BFM goal/backward map using the resulting root pose and frame c+12. Run the actor using resulting state, advanced history and raw previous action; rebuild the original 1,069 features. Apply the existing same-clock committed expert map: original tangent difference, full signed K product, correction clipped to +/-0.1 rad, saved planned target, then native target bounds. Preserve raw feedback and both clipping masks. The training target is this fixed-map target minus the new unclipped BFM base.

The label is a local frozen expert feedback response. It is not replanning or evidence that the perturbed trajectory can complete the source. Replan-boundary cases and zero gains remain explicit. Branches that hit a strict native limit retain all requested-row metadata, partial samples, first failure and actual step count. They have no complete c+1 training label and must not be silently treated as successful data. A native exception stops collection with preserved partial evidence. No relaxed limits or adaptive retry.

For useful diagnosis, one frozen-head output at each complete branch endpoint measures error against its fixed-map label. With all branches complete, the selected scope is 3,054 nominal head calls, 3,054 backward calls, 3,054 actor calls and 3,054 endpoint head calls: 12,216 graph calls total. Nominal BFM inputs and outputs already have independent exact reconstruction and immutable provenance; this design does not add a redundant nominal BFM pass. The final request must pin exact call ceilings, failed-branch handling and input hashes before execution.

Collection success would establish authentic one-control branch data, not controller qualification. A later fit would require a separately fixed objective and update budget, ordinary final export, numerical review and a fresh full canonical simulation. The original source, quiet-standing, separate hold, held-out clips and received-input timing/fault gates remain unchanged.
