# Existing native23 controller comparison — simulation only

Question: can the already trained selected native124 checkpoint provide usable
native23 PICO tracking targets for later SONIC adaptation? It is not SONIC and
cannot satisfy the current SONIC goal by changing labels. Previous native124
DadDance evidence does not qualify these complete existing PICO recordings.

Run one fixed eight-case matrix: causal q9 and current q10 reference boundaries,
each on complete saved walk002, walk003, walk008 and PICO-FreeDancing lifecycles.
q9 matches the declared causal reference/previous-torso boundary. q10 is a
separately labelled current-reference/current-torso diagnostic closer to stock
motion tracking. Both use finite differences between received q9 and q10 only;
neither consumes q11 or an optical future derivative. No source speed changes,
source resets, clip truncation, alternative joint order or absent-joint sensors.

Use exact selected checkpoint/ONNX hashes through existing pinned binding.
Use hardware23 HOME, scale and observation124, not the different legacy public
native124 policy ABI. Previous action means actually applied requested target
in this checkpoint's coordinates, including unchanged native envelope/range
projection. Inference outputs before any rejection are saved.

Native XML, masses, contacts, 2 ms integration, 50 Hz control, existing gain
arrays, joint/effort/velocity bounds and bounded inward preview stay unchanged.
Only the neural controller/reference boundary changes. Evaluate the unchanged
full standing/source/return timeline, q11 post-control fidelity thresholds and
physical bounds. A prefix or merely surviving cannot qualify a teacher.

Save all control states, substep torques/states, actual obs124 and raw actions.
Independently replay saved torque and reconstruct observations/actions. The
old referee's 994 channel is explicitly a zero-token/history compatibility
stub, not a SONIC activation. No labels, training, deployment edits or live
robot commands are authorized by this experiment. Failure is terminal per
case; no rerun-to-pass. Sensor-only/live-headset qualification remains separate.
