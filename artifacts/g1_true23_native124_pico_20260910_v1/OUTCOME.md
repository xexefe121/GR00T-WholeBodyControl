# Rejected: selected native124 is not a PICO teacher

All eight full-lifecycle requests completed with terminal failures. Causal q9
walking002/003/008 stopped after 174/159/112 source controls; current q10 stopped
after140/181/93. All six hit the simulator's absolute height/tilt stop. PICO
stopped after926/917 of5780 source controls on raw-action bounds; joint-speed
ratios reached1.03346/1.08238. No case passes tracking or admits teacher labels.

Independent audit re-integrated55,020 saved-torque2ms steps bit-exact and
reconstructed5,504 observations, reinferring every selected ONNX output exactly.
Joint-range excess0 and applied effort within existing caps do not compensate
for falls, poor tracking or velocity violations. On the same18.52s PICO prefix,
q9 native124 leg RMSE0.18235 versus SONIC0.11943rad; relative feet17.04/20.15cm
versus10.96/12.25cm. Its slightly lower root error is not a full-body improvement.
No controller swap, distillation, training labels or physical commands adopted.

Nine new scoped tests pass, including actual read-only selected-model binding.
An attempted broader legacy suite had10passes/22fixture errors: that old suite
resolves the copied WORK manifest, whose bytes differ from its fixed hash.
The new comparator explicitly binds the intact original manifest/assets and
verifies their hashes; no pinned manifest was edited to silence the old errors.
Initial new-test zero-projection assertion needed floating arithmetic tolerance
1e-15 for a1.39e-17 residual; target/physics tolerances unchanged.

First driver saved524-control walking trace before a report-writing str/Path
error. The frozen driver and saved trace remain. Versioned continuation derives
the terminal height/tilt from those arrays, writes a clearly recovered report,
and runs only the seven never-run cases. No repeated controller rollout.
Audit and all matrix processes terminal. See actual_v1/summary.json and audit.json.

Next work: correct actual SONIC training coverage. Its previous normal-core
training request explicitly says pico_used_for_training=false and uses walking
plus an unrelated dance. Existing PICO clips can now be declared optimizer
inputs, with walk008 excluded as a previously seen development check. This
changes the training split; it cannot preserve claims of PICO-held-out success.
