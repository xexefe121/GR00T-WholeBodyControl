# Full recorded walk003 replay: all recorded-source gates pass

New independent physical replay under pinned WSL MuJoCo 3.2.3 reproduced all 1,569 controls and 15,690 physics steps bit for bit: positions, velocities, actual targets and every 2 ms commanded torque. It completed all 819 source controls, entry, return and standing reference frames (31.38 s total). Native joint-bound excess was zero; maximum hardware speed ratio was 0.807809 and effort ratio was 1.0. All eight engine-warning counts and last-info values stayed zero in 15,691 samples, including initialization. These warnings are evidence from this new replay; the original producer has no warning ledger.

The native portable XML was loaded, every prepared-model array restored and checked exactly, and initialization constructed from the hash-bound v4 reference frame 10. Initial state equality with the producer was checked. No producer physical state was copied into the plant, no state was reset after initialization, and no root forces were applied. The input was the producer's recorded actual target sequence, held for ten native manual-PD physics steps each; saved feedback gains were not recomputed.

`recorded_source_audit_v2.json` was produced using the explicit local reference and `--require-pass`. All 15 gates passed. Source errors: original root p95 0.09046 m, original yaw p95 9.032 degrees, leg RMSE 0.08738 rad, original relative hands p95 0.05270/0.05802 m, original relative head p95 0.03913 m, and declared native reference root-relative feet p95 0.10097/0.09692 m. The reference includes the declared v3 retarget and v4 common-Z floor adaptation. Auditor, inspector and acceptance snapshots are included.

The last second followed a constant standing goal with root error p95 0.01536 m and final error 0.01177 m. It was not motionless: the largest absolute joint speed per physics sample had p95 2.653 rad/s and maximum 7.834 rad/s (right ankle pitch at 31.126 s, 0.26114 of its hardware velocity limit). Final maximum joint speed was 0.493 rad/s at the left knee. `saved_evidence_and_final_second_review_v1.json` preserves the full numerical review. Completion of the standing reference segment is not a claim of settled standing.

This is an offline nominal recorded-motion result. Original MPC planning p95 was 10.095 s per 100 ms commitment, with at least 740 ms reference-packet preview and up to 760 ms raw-pose support. Replaying prerecorded targets took 2.735 s of physics-loop wall time, which is not a planning benchmark. The separate Windows saved-gain feedback replay failed near control 570; this bit-exact WSL target replay does not overturn that robustness failure. Online control, received-only streaming, sensor-only control and hardware qualification remain false.

Primary evidence: `trace.npz`, `report.json`, `provenance.json`, `runner_snapshot.py`, `recorded_source_audit_v2.json`, and `saved_evidence_and_final_second_review_v1.json`. No additional dynamics were run for the final-second review.
