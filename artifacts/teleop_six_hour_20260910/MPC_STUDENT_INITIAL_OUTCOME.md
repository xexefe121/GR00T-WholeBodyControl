Initial MPC student experiment completes its implementation and closed-loop check; it does not establish working walking control.

The physical teacher is the actual MuJoCo3.2.3 bounded-feedback replay of the successful MuJoCo3.11 H30 plan, with each savedK correction explicitly clipped to±.1rad before native joint target clipping. The collector reproduces the parent's nominal physical qpos/qvel/targets bit-for-bit. Seventeen500-control physical rollouts preserve the full350-control standing/acquisition prefix and first150 source controls. Initial perturbations are bounded at3mm root position, .005rad joint position, .015rad/s joint speed; eight additional cases add .002rad joint and .01rad/s observation noise every50Hz control.

Only nominal plus3 of8 initial perturbations complete500controls with actual range excess zero. The other5 initial cases and all8 observation-noise cases fail or exceed actual joint range. All13 rejected physical traces remain preserved; none supply supervised labels. Four successful rollouts supply2000 actual pre-action observation/previous-target to applied-target pairs. Case8 is withheld as a whole rollout, leaving1500training and500validation examples. This is a previously seen source recording and offers no recording generalization evidence.

The student uses1023 floats:79 measured native proprioception values plus118 full-body goal values at explicit offsets[0,1,2,4,8,16,24,37]. The required38-frame received buffer has .74s maximum lookahead. Features include native joint/root/feet references and original29 hand/head pose/velocity intent in measured-heading coordinates. No clip identity, frame index, clock time, saved planner state or feedback gain enters the network. A256×256 ELU MLP predicts joint-target corrections relative to the current native pose reference; targets and500Hz PD torques retain native bounds.

Two thousand short CPU optimization steps give targetRMSE .0222rad on training and .0273rad on the held-out rollout. This does not translate into balance. Closed-loop nominal replay stops at292/500controls, during acquisition, after actual joint range exceeds .01135rad. Held-out initial case8 stops at280full controls plus8physical substeps when actual native speed ratio reaches1.00095. Neither reaches source playback. Traces apply student targets at every50Hz step and simulate/check every2ms; no state rewriting or root assistance occurs.

Measured full feature construction plus network inference p95 is3.04ms nominal and3.35ms case8, with no20ms misses in these failed probes. Runtime is sufficiently small for this bounded comparison; tracking remains unsuccessful. No unchanged long fit is running. The next useful evidence is physically executed successful perturbations from the new native3.2.3 replanning teacher, not synthetic labels made by applyingK to arbitrary states.

All data/checkpoints/replays live on E:\codex_sonic_runtime\mpc_student_20260910 . teacher_3s_perturbed_v1 contains all17 cases, eligibility evidence and expert_samples.npz. student_3s_fit_v1 contains training metadata/weights and replay_nominal/replay_case8. No hardware tested or authorized.
