The initial runner used the first three native landmarks (two feet and one hand)
as original hand/head points. Those original-intent metrics are invalid. All
physical replay and joint/root metrics remain valid. See sibling
`standing_executed_target_v2` for an independently rerun trace with source-aligned
neutral-wrist hand geometry and metric_revision2. This original evidence is kept
unchanged so its snapshot and hashes remain inspectable.
