The H30 controller with each feedback correction clipped to ±0.1 rad has a reproducible **partial physical result**: all 7 seconds of initialization and the first 3 seconds of walk002 run in native MuJoCo 3.2.3 without a fall or joint-range violation. Only 3 of 13.34 source seconds were tested. This result does not qualify the full recording, live teleoperation, timing, or hardware.

The independent audit reconstructs the exact law `clip_native(u_nominal + clip(K @ state_error, -.1, .1))`. It recomputes all 500 feedback targets and all 5,000 native-effort-clipped PD torques. It also re-executes the plant from its initial pose and velocity, using independently recomputed feedback; every recorded qpos and qvel value matches bit-for-bit. There are no state rewrites after initialization. Clipping activates on 70 of 500 controls. Physical joint-range excess is zero; maximum speed is 0.37118 of the native joint velocity limit. Effort reaches the native cap.

Original29 task scoring was independently reconstructed with explicit native wrist-roll hand points `(0.264, ±0.025, 0)`, original wrist-yaw hand points `(0.18, ±0.025, 0)`, and the torso head proxy `(0, 0, 0.35)`. Original29 saved hand/head positions match source FK to 3.34e-16 m. Root-relative world-axis hand/head p95 errors are 0.119618 / 0.111575 / 0.070254 m. These reproduce the corrected report. Root p95 from the report is 0.062355 m, yaw p95 4.935 degrees, and native foot p95 errors 0.059803 / 0.067089 m. The native foot metric and original29 hand/head metric have different reference geometry and should retain their names.

Evidence: `bfm_online_intent_v2/mpc_h30_323_clip1_v1/independent_audit.json`, generated by `audit_mpc_h30_clipped_replay.py`. Report and trace hashes are bound there, along with each immutable input and the runner snapshot. Planning takes 6.94 seconds at p95 for this H30 experiment; reported feedback-only timing excludes this cost. The effective source preview is conservatively 0.74 seconds because the 0.6-second MPC preview uses targets seeded by a BFM generator with 0.14-second preview. The feedback law currently uses full simulated root pose and velocity. It is an offline experiment, separate from the BFM sensor-only stream controller.

The H30 fixed-world video and six-frame contact sheet are complete and visually inspected. The robot remains upright, follows the requested heading and arm motion, and steps through the first three seconds of source motion. Foot placements and leg bends differ visibly at some samples; this is not exact pose replay. At the terminal sample, root error is 0.036 m and leg RMSE is 0.058 rad. The video labels the untested remaining 10.34 source seconds and recovery. All 501 physical boundary frames preserve their exact timestamps from 0 to 10.000 seconds, with a declared 2 ms terminal image hold; full video decoding passes. Completed media and hash-verified workspace copy receipts are in `bfm_online_intent_v2/mpc_h30_323_clip1_v1/visual_comparison_v1`. Its 117 MB of temporary PNGs remain on C.

The earlier 15-knot walk002 replay fails after 8.174 simulated seconds: 7 seconds initialization plus 1.174 source seconds. The native 3.2.3 run stops at control index 408, physics substep 7, at joint-range excess 0.0102585 rad. The 3.11 planner trace stops at the same physical boundary, with excess 0.0102256 rad. Full physics qpos differs by at most 0.00211555 between these engines. Executed-target open-loop replay falls sooner, so the saved feedback gain matters; it does not rescue this shorter-horizon run.

Its complete fixed-world video and contact sheet preserve initialization, original desired poses, source time, and the final 7/10 control boundary. The robot is stable during standing, then leans severely and collapses forward during source motion. Terminal root error is 0.950 m. This is a visible failure, not a tracking demonstration. Media are in `mjbatch_native323_replay_v1/walk002_source3_feedback_v2/visual_comparison_v1`. The video retains all 410 physical boundary frames through 8.174 seconds and has only a declared 2 ms final-image hold. Every frame timestamp matches the recorded physical clock, and the full video decodes cleanly. There are no root translations, yaw alignment, camera tracking, or time changes applied to the robot poses.

The first rendering attempt exhausted Z drive after 334 complete PNGs and one truncated image. Only this renderer's temporary frame folder was moved to the authorized C visualization directory. The resumed renderer verified and reused the complete PNGs, rerendered the truncated frame, and recorded both exact renderer snapshots in its receipt. Small completed media were copied back to the workspace; canonical render receipts retain their C paths. Original failed-attempt evidence remains intact.

The MPC math review found no coordinate/order error in the current tangent state, body FK, state cost, control target timing, or PD-to-position-servo conversion. MuJoCo tangent coordinates use world translation and right-local root rotation, followed by native joints and qvel. Cost at state knot `t` uses reference state `start+t`, while control `t` uses the next target sample. The first cost probe reads preintegration body kinematics and the supplied state; later rollout states read postintegration kinematics. These distinctions match the batch implementation.

An independent scalar-cost witness checked all 58 tangent-gradient components at three knots. Maximum error was 2.056e-6. Five free-flight directional derivative checks compared batch dynamics with manual native PD integration; maximum error was 1.150e-6. The repeat is bound to exact snapshots of the three model/MPC/iLQR source modules in `mpc_math_witness_v2_sources`, with output `mpc_independent_math_audit_v2.json`. The original witness remains preserved. Contact-active-set derivatives were not qualified by this free-flight check.

Two optimizer approximations remain explicit: planning resets acceleration warm-start at each 20 ms transition while the physical plant retains its solver state; the returned gain can be the final backward-pass gain from before the last accepted nominal rollout. Neither is a hidden physical-state injection. These are reasons to validate closed-loop physics separately, and not sufficient evidence alone to assign the observed failure to a specific cause.

The replay completion bug found during review is fixed. Previously, counting a final partial control slot could mark an incomplete source complete if the new engine did not trip the old engine's stopping threshold. `replay_completion_and_measured_arm_witness.json` executes the actual current runner's AST-extracted expressions in five synthetic boundary cases. A terminal 7/10 slot with `failure=None` cannot claim full-source or full-lifecycle completion. A fully finished source followed by a partial recovery can correctly mark source completion while lifecycle completion remains false. `plan_replayed=True` by itself means all available plan slots were replayed; it never proves the original source finished.

The original standing replay's wrong hand/head score is preserved and explicitly superseded by `standing_executed_target_v2`. New `metric_provenance_erratum.json` and `SUPERSEDED_METRIC.md` files under the original standing directory identify the invalid fields without modifying the original report, trace, or provenance. All physical qpos, qvel, target, torque, frame, and substep arrays are bit-identical between the original and corrected run. The old p95 scores near 0.62 / 0.62 / 0.57 m compared feet and a legacy hand point against original29 hand/head goals; corrected values are 0.000331 / 0.000331 / 0.010009 m for this one-second standing probe.

The measured-body arm IK branch uses actual root orientation, actual waist, actual legs, and the previous bounded arm IK pose. It adds original29 world-axis root-relative hand vectors to the current root translation, and fits the original hand rotations softly. It does not assume the newly commanded waist position was already physically reached. Actor action history receives the combined preclip target, including the arm/waist modification.

Absolute translation cancels between the IK goal and the measured-body FK. A pose-only witness removes root translation and applies additional shifts as large as 10 m; arm solutions differ by at most 1.18e-13 rad. Thus this arm component can run with its root translation set to zero, using IMU orientation with initial yaw calibration and measured joints. The BFM outer position feedback remains a separate ground-truth/odometry dependency. This witness does not qualify physical arm tracking or orientation feasibility on the five-axis arms.

One reporting distinction matters: `arm_ik_target_step_rad=0.12` and `arm_ik_joint_margin` constrain the solved IK position only. Velocity feedforward is added afterward as `(kd/kp)*dq`, followed by clipping to the original native position bounds. In the completed walk002 feedforward=1 trace, adjacent IK pose changes stay at or below 0.12 rad, while final PD target changes reach 0.410698 rad; 119 joint/step entries exceed 0.12 rad, and some targets reach native limits. The optional IK margin therefore does not establish the same margin for the final feedforward command. Physical velocity and range must continue to be scored from the plant. IK residual p95 is solver geometry before physical PD tracking, not an actual hand-tracking score.

No controller files were edited during this review. Only independent audit/render scripts, snapshots, receipts, and review artifacts were added or maintained. No robot or Pico interface was accessed.
